# CONTEXT.md

> Mon contexte personnel et professionnel pour mon Jarvis.
> Ce fichier sera rempli automatiquement lors de l'installation initiale puis mis à jour au fil du temps par Claude.

---

## Qui je suis

- **Prénom :** Jaslin
- **Ville / Pays :** Originaire de Gros-Morne, vit actuellement à Pignon (Haïti), avec des allers-retours entre les deux villes
- **Situation actuelle :** Étudiant en sciences informatiques, développeur fullstack freelance et professeur de programmation
- **Profil dominant :** Mix (indépendant / freelance + étudiant + enseignant)

---

## Ce que je fais

### Activité principale

Je développe des applications web et mobile pour tous types de clients (entreprises, particuliers, ONG), rémunéré au projet. En parallèle, je poursuis mes études en sciences informatiques et j'enseigne la programmation.

### Détails selon le profil

**Études :**
- Domaine d'études : Sciences informatiques
- Niveau / Année : Études universitaires en cours

**Activité freelance (développement) :**
- Activité : Développement d'applications web et mobile, solutions informatiques sur mesure
- Modèle économique : Rémunération au projet
- Clients types : Entreprises, particuliers, ONG

**Enseignement :**
- Professeur de programmation

---

## Mes objectifs

### Objectifs court terme (3 à 6 mois)

- Développer et lancer une plateforme e-learning destinée aux étudiants et professionnels (cours, évaluations, suivi des progrès)
- Concevoir une application complète de gestion hospitalière (patients, rendez-vous, dossiers médicaux, facturation, utilisateurs)
- Développer des solutions SaaS de gestion pour établissements scolaires et banques, et obtenir les premiers clients pour commencer le déploiement
- En parallèle : progresser dans les études, développer l'activité freelance et renforcer les compétences en développement web et mobile

### Objectifs long terme (1 à 3 ans)

- Terminer les études universitaires et être reconnu comme un professionnel compétent en développement logiciel et systèmes d'information
- Transformer les projets SaaS en entreprises rentables (e-learning, gestion hospitalière, gestion scolaire, solutions financières)
- Disposer d'un portefeuille de clients fidèles en Haïti et à l'international, avec des revenus récurrents
- Créer une entreprise technologique solide capable de développer des solutions innovantes pour organisations, écoles, hôpitaux et institutions financières
- Atteindre l'indépendance financière, investir dans de nouveaux projets, former d'autres développeurs et avoir un impact positif en Haïti grâce à la technologie

---

## Mes projets en cours

Liste des projets ou chantiers actifs sur lesquels je veux que Claude m'aide :

- Mon activité d'enseignement de la programmation
- **Manuels réseau** (`livrables/reseau/`) : document de formation pour un réseau (informatique), généré via un script Node.js (`docx` npm, sections découpées en modules 1-5/6-10/11-15). Deux manuels produits : `Manuel-Reseau-Pro-v2.docx` et `Manuel-Reseau-Videosurveillance.docx`. Découvert non documenté en `/prime` le 2026-07-07, clarifié avec Jaslin : support de formation, pas un projet logiciel.
- **EduSpher** : plateforme e-learning (Next.js 14 App Router + NextAuth v5 + Prisma + SQLite). UI complète (landing, auth, dashboards étudiant/formateur/admin, lecteur de cours, quiz, certificats). Phases 1, 2 et 3c livrées : DB SQLite seedée (6 cours, 3 users démo), 20+ API routes, dashboards branchés sur vraies données, persistance progression leçons, quizzes sauvegardés, course builder formateur (CRUD cours + publication), upload vidéo/PDF pour le contenu des leçons. Phase 3c : éditeur de contenu formateur, flow d'inscription étudiant avec gate (cours gratuits/payants), Stripe Checkout + webhook, dashboard admin avec vraies stats Prisma, SettingsPage branchée sur le profil réel. **Dashboards complétés (2026-07-03)** : tous les items de sidebar (formateur, admin, étudiant) menaient auparavant à des redirections silencieuses vers le dashboard principal ou affichaient des données mockées du prototype d'origine — corrigé intégralement. Navigation : chaque clé de menu route vers sa vraie page. Notifications et certificats branchés sur de vraies données Prisma (génération auto d'un certificat + notification à 100% de progression). Nouvelles pages formateur (Étudiants, Revenus, Avis), admin (Utilisateurs avec changement de rôle, Cours avec publier/dépublier, Revenus), étudiant (Explorer avec recherche/filtre catégorie). Système de messagerie 1:1 étudiant↔formateur construit de zéro (modèles Prisma Conversation/Message, API, UI par polling — pas de Socket.io, choix fait pour rester compatible avec un futur déploiement Vercel serverless), badge non-lus dynamique. Système d'avis construit (soumission étudiant sur la page cours, agrégation côté formateur). Série d'activité (streak) réelle remplaçant le widget figé, déclenchée par complétion de leçon ou soumission de quiz (modèle ActivityLog). Comptes démo : julien@eduspher.com / sofia@eduspher.com / admin@eduspher.com (password123). **Plus aucune donnée mockée** : widget "Objectif de la semaine" (dashboard étudiant) et catalogue de la landing page branchés sur les vraies données Prisma (2026-07-03 soir), `lib/data.js` supprimé. Toujours en SQLite local (migration PostgreSQL/Supabase + déploiement Vercel repoussés, non prioritaires pour l'instant).
- **SYGS-IMFP** : système de gestion scolaire (Institution Mixte Faustin 1er). Projet avancé (60+ commits) : backend Express 5 + TypeScript + Prisma + MySQL, frontend Vite + React 18 + shadcn/ui. Couvre élèves, profs, notes, bulletins, présence, frais de scolarité, etc. Architecture multi-tenant : modèle Silo décidé (une instance + une base par école). Refactor vers le partagé jugé prématuré avant validation du marché. Module Messagerie complet (2026-06-30) : Socket.io, conversations directes et groupes, temps réel, badge non-lus dans la sidebar, page dédiée full-height. Pièces jointes : photos (miniature cliquable), documents (icône + téléchargement), messages vocaux (MediaRecorder + lecteur audio natif), upload via POST /api/messages/conversations/:id/attachment (multer, 10 Mo max). Accusés style WhatsApp : ✓ Envoyé / ✓✓ gris Livré / ✓✓ bleu Lu, mis à jour en temps réel via Socket.io (ack_delivered → message_status ; markAsRead diffuse aussi message_status). En groupe, "Lu" = tous les membres ont vu. Présence en ligne (point vert, "En ligne"/"Hors ligne"), indicateur de frappe, sécurité join_conversation (vérification appartenance avant socket.join), errorHandler global branché.
- **KONEKTE** : plateforme de rencontres haïtienne, déployée en production. Stack : Next.js 16 + Express + MySQL + Prisma + Socket.io. URLs : konekte-xi.vercel.app (frontend) / jarvis-starter-kit-production-f573.up.railway.app (backend). Fonctionnalités : swipe avec drag, matching, chat (texte, voix, photos), notifications temps réel, "Qui m'a liké", Super Likes, Premium avec Stripe (fonctionnel) + MonCash (prêt, en attente credentials Digicel), emails transactionnels Gmail SMTP, Cloudinary pour les médias, vérification d'email. **Durcissement sécurité complet (2026-07-03 nuit)** suite à un audit senior dev : RBAC admin (champ `isAdmin` inexistant avant, n'importe quel compte pouvait bannir/lire les signalements), callback MonCash et webhook Stripe rendus idempotents (rejeu ne pouvait plus doubler l'activation Premium), fuite de données sur "Qui m'a liké" corrigée (flou appliqué côté serveur via Cloudinary, plus seulement en CSS), quota Super Like imposé côté backend, validation Zod (contrôle 18 ans) enfin branchée sur les vraies routes, rate limiting login/register, index Prisma ajoutés. Auth migrée de JWT unique 7j en localStorage vers access token court (15 min) + refresh token opaque en cookie httpOnly avec révocation réelle en base (RefreshToken, rotation). Socket.io équipé d'un adapter Redis (ioredis) pour le scaling horizontal Railway, testé avec deux instances backend séparées. Prochaine étape : credentials MonCash Digicel Business Haiti ; tests automatisés toujours absents.
- **BANKA** : système de gestion bancaire complet. Stack : Next.js App Router + Express 4 + TypeScript + Prisma v5 + PostgreSQL. Ports : 4001 (backend) / 3001 (frontend). Modules livrés : Tableau de bord, Clients (KYC renforcé : pièce d'identité + numéro obligatoires pour INDIVIDUEL, âge ≥ 18 validé, soft delete avec guards), Comptes (9 types : EPARGNE, COURANT, TERME, JOINT, MICRO_EPARGNE, TONTINE, RETRAITE, JEUNESSE, CREDIT, clôture avec guards : solde=0 + pas de prêts actifs + pas d'épargnes programmées), Transactions (dépôt/retrait/virement atomiques avec compare-and-swap), Caisse (sessions journalières avec auto-liaison des transactions à la session ouverte), Crédits & Prêts (amortissement dégressif/constant/in fine, décaissement, remboursement, remboursement anticipé avec recalcul du tableau d'amortissement, annulation avant décaissement), Rapports (journalier, PAR 30/90, impayés), Rapport BRH (ratios prudentiels liquidité ≥ 20% / solvabilité ≥ 8%, grandes expositions top 5, comptes capitaux), Journal d'audit, Utilisateurs, Agences (avec compteur d'employés RH), Mandats & Procurations, Administration (paramètres configurables), RH complet (postes, employés, contrats, congés, bulletin de paie BROUILLON→VALIDÉ→PAYÉ, avances sur salaire, éléments variables, pointage biométrique ZKTeco ADMS), Taux de change (CRUD taux HTG/USD, virement cross-devise atomique), Alertes AML (4 détecteurs : seuil déclarable, structuration, vélocité élevée, mandataire blacklisté), Épargne programmée (virements automatiques récurrents multi-fréquences), Comptabilité SYSCOHADA complète (dashboard comptable, plan comptable 31 comptes classes 1/2/4/5/6/7, journal avec saisie manuelle, grand livre par compte, bilan actif/passif, compte de résultat produits/charges, réconciliation écritures en échec, clôture comptable mensuelle avec verrouillage des périodes). Fonctionnalités transversales : RBAC 7 rôles, audit log complet, frais automatiques (tenue de compte mensuel, dossier prêt 2%, virement 0,5%), notifications temps réel SSE (cloche header, flash ALERTE_AML), double-entry bookkeeping automatique sur toutes les opérations, pénalités de retard, PDF dossier crédit, montants compacts K/M/Md, blocage transactions sans caisse ouverte. Sécurité renforcée (session 2026-06-29) : JWT refresh secret obligatoire, RBAC sur toutes les routes, atomicité transactions avec compare-and-swap, politique mot de passe 12 car minimum (maj+min+chiffre+spécial), révocation sessions sur désactivation compte, reset mot de passe par email (token opaque 256-bit, usage unique, 1h expiry), rate limiting sur endpoints sensibles, whitelist droits mandats, soft delete partout. Page login redesignée (fond sombre navy, carte deux panneaux semi-transparente, tips bancaires carrousel). Bilan comptable déséquilibré (signalé 2026-07-02, bloquant avant démo client) corrigé et vérifié le 2026-07-03 : root cause cumulée (signe des soldes, comptes capitaux mal typés, résultat de l'exercice jamais intégré, plan comptable dupliqué dans le seed sans dotation initiale, ouverture de compte avec solde initial sans écriture comptable). Audit sécurité approfondi + roadmap client 15 points (2026-07-12) : 6 failles corrigées (mass assignment RH, double paiement salaire, épargne programmée ouverte à tout rôle, RBAC agence incomplet, commKey biométrique en clair), architecture multi-agence centralisée livrée (traçabilité transaction par agence, solde de caisse persistant avec plafond d'alerte, transferts de trésorerie inter-agences), bug prêt employé/paie corrigé (la retenue sur salaire ne mettait jamais à jour le prêt), module Documents/KYC livré (upload local, expiration). Clôture comptable mensuelle livrée (2026-07-12, suite) : verrouillage d'une période (format YYYY-MM) empêchant toute saisie manuelle ou suppression d'écriture rétroactive, sans jamais bloquer les écritures automatiques des opérations bancaires (toujours datées du jour) ; clôture refusée si le bilan n'est pas équilibré à cette date sauf forçage explicite réservé au SUPER_ADMIN ; réouverture également réservée au SUPER_ADMIN, tracée ; compare-and-swap anti double-clôture concurrente. Page `/compta/cloture` + avertissement visuel sur le formulaire de saisie du journal. Vérifié en conditions réelles (API + Playwright). Détail complet dans `context/HISTORY.md`. En cours de développement actif. **10 des 15 points de la roadmap client restent à faire** (RSE, statut compte lié au prêt, rapprochement bancaire BRH, chèques, PCD, backup infra, interbancaire, export compta, API externes, messagerie interne). Commité en git (auto-commit).
- **GESCOM** : ERP commercial pour un client signé (entreprise avec 1 boutique détail + 1 entrepôt grossiste, stocks séparés, devise HTG, 5-20 utilisateurs). Stack : Next.js App Router + Express 4 + TypeScript + Prisma v5 + PostgreSQL. Ports : 4002 (backend) / 3003 (frontend). Phases 0 à 3 livrées (socle, Stock/Produits, Ventes/Clients, Achats/Fournisseurs avec réception partielle + recalcul CUMP). Modules en prod : Tableau de bord premium (KPI temps réel avec tendance vs veille, ventes 7 derniers jours, top produits vendus, clients à risque par solde dû, commandes fournisseur en retard, actions rapides par rôle avec modals dédiés), Produits (CRUD + alertes seuil), Stock (ajustement atomique compare-and-swap, mouvements, alertes), Ventes (création POS atomique avec compare-and-swap anti-survente + écriture compta + soldeDu CREDIT, annulation avec restitution, historique), Clients (CRUD PARTICULIER/GROSSISTE, solde dû), Achats/Fournisseurs (commandes brouillon→envoyée→reçue, réception ligne par ligne). Design : thème vert, Inter, sidebar icônes + drawer mobile, header dropdown, responsive 100%. Compte démo : admin@gescom.ht / Admin@123. Sécurité : race condition sur le décrément de stock corrigée (2026-07-01) après revue senior dev ayant identifié le même risque que sur BANKA. Transferts inter-sites (Ph4) livrés (2026-07-02) : transfert boutique↔entrepôt avec statuts EN_TRANSIT/RECU/ANNULE, décrément atomique à la création (CAS), incrément à la réception, restitution à l'annulation, garde anti double-traitement. Comptabilité (Ph5) livrée (2026-07-02 soir) : journal avec saisie manuelle, grand livre par compte, bilan actif/passif, compte de résultat produits/charges, réconciliation des écritures en échec (bug corrigé au passage : achat.service.ts perdait silencieusement les écritures échouées au lieu de les tracer). Refonte design system frontend en parallèle (composants Badge/StatCard/PageToolbar/EmptyState, classe table-shell) appliquée à toute l'application. Toutes phases vérifiées via API (curl) ou tsc, UI non testée visuellement (pas de navigateur disponible dans l'environnement). Rapports (Ph6) livré (2026-07-03) : 4 rapports agrégés (ventes filtrable par période avec évolution/top produits/top clients/marge estimée, stock avec valorisation et rotation 90j, achats/fournisseurs avec taux de réception, clients avec encours crédit), RBAC réservé SUPER_ADMIN/GERANT, page à onglets suivant le pattern Compta. **Roadmap Ph0-6 de GESCOM entièrement clôturée.** Flux complet vérifié en navigateur (Playwright installé à la volée dans l'environnement, jamais fait auparavant faute d'outil) : Rapports, Transferts et Compta rendent tous correctement, aucune erreur bloquante. **Bilan comptable vérifié équilibré le 2026-08-09** : requête directe de la base réelle (13 écritures) + appel de la vraie fonction `getBilan()` confirmant Actif = Passif = 30 041,2 HTG (`equilibre: true`). L'écart signalé fin juillet (Actif ≠ Passif) était déjà résorbé par une écriture manuelle datée du 2026-08-03 dans le journal ("Correction : annulation VNT-000003 jamais contrepassée avant le fix du 2026-08-03") — une vente annulée dont la contre-passation comptable n'avait jamais été enregistrée, corrigée en parallèle par Jaslin dans son IDE. Reste : tests automatisés (toujours zéro sur le projet). Nom de code provisoire, renommable.
- **LAKAY** : plateforme immobilière haïtienne (annonces de biens à louer/vendre). Stack : Next.js App Router + Express 4 + TypeScript + Prisma + PostgreSQL + PostGIS + Redis + BullMQ + Socket.IO. Ports : 4003 (backend) / 3004 (frontend). 9 types de biens (chambre, studio, appartement, maison, villa, terrain, local commercial, bureau, entrepôt), 7 rôles RBAC (Super Admin à Visiteur), recherche avancée 30+ filtres avec critères Haïti-spécifiques (eau/électricité/générateur/citerne/panneau solaire), système de messagerie temps réel (fix décalage format API data.data), abonnements (FREE/BASIC/PROFESSIONAL/ENTERPRISE) avec modal de souscription MonCash, paiements MonCash + Stripe, module IA (estimation de prix, génération de descriptions, recherche langage naturel). Fonctionnalités récentes : PropertyCard redesignée avec overlay gradient et animations, hero full-width avec image de fond, stats homepage dynamiques (API /stats), départements dynamiques (10 départements + groupBy Prisma), FAQ accordion, badges non-lus (messages/notifications/favoris) dans le header via useNavCounts, GPS picker carte Leaflet dans le formulaire de création d'annonce, fix 422 édition annonce (sanitize ALLOWED fields), fix 401 admin review (requireAuth manquant), fix 500 admin stats (Subscription.isActive vs status), fix SMTP non-bloquant. Comptes seed : admin@lakay.ht / Admin@Lakay2024!, proprietaire@demo.ht / Owner@123. **Audit senior dev + 4 correctifs (2026-07-08)** : refresh token enfin migré en cookie httpOnly avec succès (une tentative précédente début juillet avait été annulée à cause d'une race condition entre le rafraîchissement automatique au démarrage de l'app et celui de l'intercepteur Axios, cette fois corrigée par un verrou partagé — vérifié en navigateur réel, y compris après rechargement de page) ; `favoriteCount` ne peut plus devenir négatif ; un paiement Stripe/MonCash/NatCash abandonné ne bloque plus les tentatives suivantes (nouveau job horaire de nettoyage) ; les visites confirmées basculent désormais automatiquement en "effectuée" une fois leur date passée, débloquant la possibilité de laisser un avis. En développement actif.
- **MEDIKA** : application de gestion hospitalière (patients, consultations, examens médicaux, file d'attente, rendez-vous, facturation, hospitalisations, pharmacie, planning du personnel). Stack : Next.js 15 App Router + Express 4 + TypeScript + Prisma v5 + PostgreSQL, shadcn/ui (sur Base UI). Modules construits : patients, file d'attente (numérotation journalière, workflow EN_ATTENTE → EN_CONSULTATION → TERMINE), consultations (workflow en 2 visites : plainte + examens à V1, diagnostic + prescriptions + prochain RDV à V2), examens médicaux (formulaires structurés par type avec normes et détection de valeurs anormales), hospitalisations (séjours, chambres/lits, prescriptions structurées, administrations infirmières, dossier patient complet), pharmacie (inventaire, lots, mouvements, dispensation liée aux prescriptions, dispensation ambulatoire directe, alertes stock, commandes fournisseurs), planning du personnel (gardes CRUD, absences CRUD, vue semaine, disponibilité, vue personnelle par utilisateur connecté). Dernières fonctionnalités ajoutées : picker médicament avec auto-remplissage du dosage, prescription builder structuré, notifications d'administration médicaments (SSE), auto-création de rendez-vous de suivi, seed 63 médicaments, facturation hospitalière, dashboard enrichi (lits/recettes), export dossier médical PDF complet depuis la page patient (identité, antécédents, consultations, examens, hospitalisations, prescriptions, factures), recherche globale Cmd+K (SearchPalette debounce 280ms, navigation clavier, patients/factures/examens via GET /api/search), rapports avec sélecteur de période (5 presets + dates personnalisées, paramètres from/to), section examens dans les rapports journaliers. En cours de développement actif.
- **SHOPAY** : plateforme SaaS e-commerce multi-tenant (store builder, façon Shopify en plus petit, pour marchands haïtiens). Stack : Express 4 + TypeScript + Prisma v5 + PostgreSQL (backend, port 4005) + Next.js App Router (frontend, port 3006). Découvert en `/prime` le 2026-07-07 (dossier jamais commité ni documenté, migration Prisma datée du jour même) — projet déjà largement codé, probablement une session Claude Code antérieure interrompue avant `/update`. Modèle multi-tenant : `Boutique` comme tenant, `boutiqueId` posé et indexé sur les tables filtrées directement (Product, Category, Order, Cart, Customer, AuditLog, Notification, Payment nullable), tables toujours accédées via leur parent (OrderItem, CartItem, ProductVariant, ProductImage) sans `boutiqueId` redondant. Isolation garantie par `resolveBoutique` (middleware) : la boutique effective vient toujours du token JWT, jamais d'un paramètre client, empêchant un `BOUTIQUE_STAFF/OWNER` de trafiquer l'URL pour accéder à une autre boutique. 3 rôles (PLATFORM_SUPER_ADMIN / BOUTIQUE_OWNER / BOUTIQUE_STAFF), plans marchands FREE/BASIC/PRO avec quotas. Modules backend : auth, boutiques, catalog (produits + variantes + catégories), cart, orders (privé + public checkout invité), payments (Stripe + MonCash + preuve manuelle, mêmes patterns que LAKAY/POSTA), billing (abonnement plateforme + quotas), storefront (vitrine publique par boutique), notifications, admin (plateforme). Frontend : zone marchand `(dashboard)` (produits, catégories, commandes, facturation, paramètres), zone admin plateforme (boutiques, paiements, stats), vitrine publique `/store/[slug]` (page boutique, produit, panier, checkout). Champs Haïti réutilisés du reste du portefeuille (Department enum, landmark, devise HTG). **Jamais commité en git.** Audit de complétude effectué le 2026-07-07 : RBAC, rate limiting, idempotence des paiements (compare-and-swap), signature webhook Stripe, audit log et quota produits déjà corrects. Failles trouvées : aucun flow email de vérification/mot de passe oublié, MonCash non réellement intégré (`// TODO` avec URL de redirection factice), et surtout **aucune vérification de stock au checkout** (une commande pouvait être créée sans contrôle du disponible, seul le décrément après paiement existait, sans garde). **Corrigé et vérifié en conditions réelles la même session** (API réelle, pas juste lecture de code) : vérification de stock ajoutée dans `orders.service.ts::checkout()` (rejet 409 si quantité demandée > stock, avant même la création de la commande) + garde compare-and-swap ajoutée dans `payments.service.ts::activateOrder()` (le décrément à l'activation du paiement échoue proprement — toute la transaction, y compris le paiement, est annulée — si le stock est entretemps devenu insuffisant, au lieu de laisser `stockQty` passer en négatif). Testé via l'API réelle (curl, backend lancé localement avec la vraie base Postgres `shopay`) : commande refusée avec message clair quand la quantité dépasse le stock disponible, commande normale acceptée, et paiement refusé avec rollback complet (stock inchangé, paiement resté PENDING) quand le stock est artificiellement épuisé entre la commande et l'activation du paiement. Données de test nettoyées après vérification. **Reste à corriger** (hors périmètre de cette intervention) : email de vérification/mot de passe oublié absent, intégration MonCash réelle à faire, notifications `ORDER_PLACED`/`LOW_STOCK`/`PAYMENT_PROOF_SUBMITTED` jamais déclenchées malgré leur modélisation, quota limité au nombre de produits, pas de coupons/remboursements/avis, taux de change HTG→USD codé en dur. **Découverte annexe :** un serveur backend ET un serveur frontend Next.js (port 3006) de SHOPAY tournaient déjà en arrière-plan avant même le début de cette session (zombies d'une session antérieure, jamais arrêtés) — non touchés (à part l'instance backend lancée puis arrêtée pour la vérification), à nettoyer par Jaslin s'il le souhaite. **Confirmé plus tard dans la même session : Jaslin travaillait effectivement en parallèle dans son IDE sur ces mêmes serveurs** (quotas de facturation, `plans.ts`/`billing.*`), même schéma que sur GESCOM Phase 5.

**Marketplace de découverte ajoutée (même session, sur demande explicite de Jaslin qui imaginait un modèle façon Amazon) :** clarification faite avant de coder — vrai panier unifié multi-vendeur écarté (aurait exigé une refonte Order/Cart/Payment pour répartir un paiement entre plusieurs marchands, sur-ingénierie sans un seul marchand réel actif) au profit d'une **découverte centralisée avec panier restant scopé par boutique** (façon Etsy) : nouveau module backend `marketplace/` (`GET /api/marketplace/products` avec recherche texte + filtre département + pagination, `GET /api/marketplace/boutiques`), nouvelle page frontend `/marketplace` (recherche, filtre département, grille produits avec lien vers la fiche produit de la boutique vendeuse), lien "Découvrir les boutiques" ajouté à la landing page. Vérifié via l'API réelle (recherche, filtre département, département invalide ignoré proprement) et rendu de la page confirmé (200) sur le serveur frontend déjà en cours d'exécution.
- **POSTA** : plateforme de mail personnalisé avec nom de domaine (créer des adresses email professionnelles type contact@sonentreprise.ht). Stack : Express + TypeScript + Prisma + PostgreSQL (backend, port 4004) + Next.js + Tailwind (frontend, port 3005). Découvert en cours de session le 2026-07-07 (projet déjà entamé, non documenté), puis entièrement complété la même session. Modèles : Utilisateur (SUPER_ADMIN opérateur de la plateforme / CLIENT_ADMIN propriétaire de domaines), Domain (génération auto de clé DKIM 2048 bits, vérification DNS MX/SPF/DKIM/DMARC réelle via requêtes DNS), Mailbox (hash SHA512-CRYPT compatible Dovecot, jamais bcrypt), Alias. Postfix/Dovecot ne touchent jamais les tables applicatives : trois vues SQL en lecture seule (mail_domains/mail_mailboxes/mail_aliases, un domaine n'est éligible au mail qu'une fois vérifié) accessibles via deux rôles Postgres à privilèges minimaux (postfix_ro sans accès à la colonne mot de passe, dovecot_ro avec), le tout documenté dans `docs/MAIL_SERVER_SETUP.md` (config Postfix/Dovecot/OpenDKIM prête à déployer, **aucun VPS mail encore provisionné** — c'est le seul chantier vraiment bloquant avant un premier client réel). Auth par cookies httpOnly + refresh token (30 j, sans rotation). Mot de passe oublié et invitation client par email (nodemailer, no-op gracieux si SMTP non configuré, contenu loggé en dev faute de SMTP réel) : un SUPER_ADMIN crée un compte CLIENT_ADMIN sans jamais connaître son mot de passe, le client le choisit via un lien à usage unique. Journal d'audit réellement branché sur les actions sensibles (connexion, CRUD domaines/boîtes/alias, activation compte, paiements). Plans/quotas/facturation : 4 plans (FREE/STARTER/PRO/BUSINESS, prix HTG et limites domaines/boîtes mail/stockage validés avec Jaslin) avec quotas réellement appliqués à la création (pas juste affichés, bug de quota par défaut non appliqué trouvé et corrigé en cours de session), paiement MonCash manuel (preuve + validation admin, comme LAKAY) et Stripe Checkout + webhook idempotent (clés réelles non disponibles dans l'environnement, jamais testé en conditions réelles contrairement au reste). Landing page publique à la racine avec tarifs réels et CTA "nous contacter" par email (onboarding manuel assumé tant que le VPS mail n'existe pas, pas d'auto-inscription), dashboard authentifié déplacé sous `/app`. Tout vérifié en navigateur à chaque étape (Playwright installé à la volée, jamais de test automatisé). Comptes seed : admin@posta.ht / ChangeMoi123!.

  **Audit sécurité + 2 correctifs (2026-07-21)** sur « vérifiez POSTA et donnez-moi votre avis ». Lecture intégrale du backend (petit et propre) : **verdict, projet le mieux tenu du portefeuille côté sécurité de base, aucune faille exploitable trouvée**. Confirmés solides : isolation Postfix/Dovecot exemplaire (3 vues en lecture seule + `GRANT SELECT` colonne par colonne excluant le hash de mot de passe pour `postfix_ro`, les rôles mail n'ayant aucun accès aux tables applicatives), clé privée DKIM jamais en base (écrite 0600 sur disque), séparation des hash (SHA512-CRYPT boîtes / bcrypt panel), contrôle de propriété `assertAccess` systématique, Stripe (webhook corps brut avant `express.json`, signature vérifiée, idempotence CAS) et MonCash approve/reject idempotents, reset mot de passe (jeton opaque haché SHA-256, usage unique, 1 h, révoque les sessions, réponse neutre), `authLimiter` câblé (10/15 min, `skipSuccessfulRequests`) sur login/forgot/reset, secrets JWT ≥ 32 exigés au boot, pas d'auto-verrouillage (un SUPER_ADMIN ne peut pas se désactiver, la cible doit être CLIENT_ADMIN), intercepteur frontend avec verrou partagé `refreshPromise` (la race corrigée sur LAKAY). **Deux correctifs appliqués et vérifiés en conditions réelles (backend lancé sur la vraie base Postgres `posta`)** : (1) **refresh tokens stockés en SHA-256** au lieu du JWT brut (login/refresh/logout), alignement sur BANKA/LAKAY/ANTENN — un dump de base ne livre plus de jeton de session réutilisable ; vérifié que le token en base = 64 hex = SHA-256 du cookie, cycle refresh/logout/réutilisation 200/200/401, 17 anciens tokens bruts purgés. (2) **`validate.middleware.ts` réassigne enfin le résultat parsé** (`req.body` remplacé par la version validée et strippée, `query`/`params` fusionnés par `Object.assign`) — même landmine que ANTENN (#1 mass assignment) et OTELA (coercion), **inerte jusqu'ici sur POSTA** (tous les services prennent des paramètres nommés, aucun ne dépend d'une coercion Zod) mais corrigée préventivement ; non-régression vérifiée (création domaine, boîte mail sur route imbriquée avec `params`, rejets 422). `tsc --noEmit` propre, base remise à 0 session, serveur arrêté par PID précis. Points mineurs restants non corrigés (documentés) : pas de rotation du refresh token, `moncash/initiate` peut créer des paiements EN_ATTENTE en série, taux HTG→USD Stripe codé en dur (130), `SameSite=Lax` à revoir si frontend/API cross-site en prod. **Le vrai blocage reste le VPS mail jamais provisionné.** Commité en git (`25b63fd`).
- **ANTENN** : système de régie pour une chaîne de streaming FAST (Free Ad-supported Streaming TV, façon chaîne TV linéaire) pour un client de Haitech Solutions. Construit de zéro le 2026-07-08 sur commande détaillée du client, en respectant l'ordre imposé (modèle de données → grille → sponsors → player web). Stack : Express 4 + TypeScript + Prisma v5 + PostgreSQL (backend, port 4006) + Next.js App Router (frontend, port 3007, inclut aussi le player public). Le moteur de playout (ErsatzTV, open source) et l'ingest RTMP terrain sont des briques externes existantes, volontairement non recodées — seuls les points d'API sont documentés (`backend/src/integrations/ersatztv.ts`) et prévus en config (`ERSATZTV_BASE_URL`, `CDN_BASE_URL`), sans rien simuler. Modèle de données : Contenu, CreneauGrille (programmé/direct/pub, avec `syncStatus` BROUILLON/SYNCHRONISE distinguant explicitement une modification en cours d'édition de ce qui est réellement à l'antenne — exigence explicite du client), Match (statut piloté manuellement Démarrer/Terminer, pas de détection auto de l'ingest tant qu'ErsatzTV n'est pas branché), Sponsor (alerte contrat expirant sous 30 jours), IncrustationLogo/BandeauSponsor (habillage), DiffusionLog (base des rapports). RBAC 2 rôles : ADMINISTRATEUR (accès complet, seul à pouvoir modifier les contrats sponsors) et OPERATEUR_REGIE (gère grille/matchs au quotidien, lecture seule sur Sponsors). Immutabilité de l'historique : un créneau déjà diffusé (fin < maintenant) est refusé en modification/suppression (409), vérifié côté service pas juste côté UI. Premier projet du portefeuille avec un dashboard admin entièrement dark mode (nouveau thème navy/cyan/or, tous les autres projets n'ayant du dark mode que sur leur écran de login) — composants Badge/StatCard/PageToolbar/EmptyState/Modal réutilisés du design system GESCOM, seuls les tokens de couleur changent. Grille : timeline horaire visuelle + liste, bandeau persistant "N créneaux en brouillon" vs "Grille synchronisée". Player public `/regarder` : hls.js avec fallback natif Safari, EPG (en ce moment/à suivre) lu uniquement depuis les créneaux SYNCHRONISE (jamais un brouillon), badge "EN DIRECT" animé, overlay logo/bandeau en HTML par-dessus la vidéo (les deux approches possibles — overlay HTML vs incrustation brûlée côté playout — documentées avec leurs compromis dans le composant), indicateur de qualité réseau (Network Information API, dégradation propre si non supporté). Durci en cours de session : même correctif que LAKAY (2026-07-08) appliqué préventivement — verrou partagé sur le rafraîchissement de token pour éviter la race condition entre le hydrate() du layout et l'intercepteur Axios. Vérifié en conditions réelles : API (curl — guard 409 sur créneau passé, RBAC 403, duplication, contrat expirant), navigateur (connexion, RBAC visuel par rôle, création de créneau via le vrai formulaire, toutes les pages), et lecture HLS réelle testée avec un flux de démonstration public (Apple bipbop) avant d'être retirée de la config (aucun CDN client réel disponible). `tsc --noEmit` propre. Comptes seed : admin@antenn.ht / operateur@antenn.ht, mot de passe Antenn@123. **Reste à faire** (documenté, pas codé — hors périmètre) : intégration API ErsatzTV réelle, provisioning CDN HLS (Bunny Stream/Cloudflare Stream), génération d'URL RTMP par le serveur de streaming.

**Audit senior dev + designer et chantier correctif (2026-08-14)** sur « analyse ANTENN et dis-moi ce qui manque », puis « vas-y » (portée assumée : les 6 points de l'audit enchaînés). Le projet avait entretemps grossi (replay/VOD, EPG multi-jours, config chaîne, app mobile Expo). **Verdict de l'audit : le code métier était le mieux commenté du portefeuille et ses invariants réellement gardés côté service, mais le produit ne savait pas justifier sa raison d'être commerciale.** Trois manques structurels trouvés et corrigés. (1) **`DiffusionLog` n'était jamais écrit** (une seule occurrence dans tout le backend, un `findMany` en lecture) : le rapport sponsors, seul livrable qui justifie une facturation sur un modèle FAST, retournait structurellement zéro diffusion et zéro vue, et aucune source d'audience n'existait (ni le player web ni le mobile n'envoyaient quoi que ce soit). Chaîne complète construite : nouveau modèle `AudienceSession` (une ligne par couple session/créneau, clé opaque sans donnée personnelle) alimenté par un heartbeat de 30 s des deux players, durée recalculée côté serveur et plafonnée à 60 s par ping (un onglet en veille n'ajoute jamais une heure fictive), ping refusé sur un brouillon ou hors plage de diffusion, génération paresseuse et idempotente des `DiffusionLog` (unicité base sur `creneauId`) déclenchée à l'ouverture du moniteur et des rapports faute de tâche de fond, purge des sessions brutes à 90 jours. Le compteur de vues replay, jusque-là gonflable par une boucle curl sur une route publique sans déduplication, passe par le même mécanisme (une vue par session) et l'ancienne route a été retirée. Limiteur de débit dédié et généreux sur le ping, explicitement séparé du limiteur global : en Haïti une IP publique couvre couramment tout un quartier ou un cybercafé, plafonner l'audience par IP aurait faussé le rapport sponsor. (2) **RBAC quasi absent au-delà des sponsors** : `requireAdministrateur` n'était câblé que sur les contrats, si bien qu'un OPERATEUR_REGIE pouvait changer l'identité de la chaîne et surtout supprimer les incrustations et bandeaux sponsors — l'exécution même du contrat commercial. Ligne de partage posée et documentée (l'opérateur exploite, l'administrateur contracte et détruit) : création d'habillage ouverte aux deux rôles, suppression réservée à l'administrateur, idem contenus et suppression définitive de replay, identité de chaîne en lecture seule pour l'opérateur, et sidebar filtrée par rôle. (3) **Aucun journal d'audit** alors que le produit facture une exposition contractuelle : modèle `AuditLog` (auteur figé en clair pour survivre à la suppression d'un compte, aucune route d'écriture ni de suppression exposée), 20 actions tracées, page `/journal` réservée à l'administrateur. **Gestion des utilisateurs livrée** : elle était annoncée dans le README et dans un commentaire du middleware RBAC alors qu'aucun module n'existait — seul le seed créait des comptes, un opérateur perdant son mot de passe était bloqué jusqu'à une intervention directe en base. Module complet réservé à l'administrateur (création, changement de rôle, activation/désactivation révoquant immédiatement les sessions, anti auto-verrouillage, garde du dernier administrateur actif, pas de suppression pour ne pas rompre le lien avec le journal d'audit) et **lien de réinitialisation à usage unique** (jeton haché SHA-256, 1 h) transmis par le canal de son choix faute de SMTP, l'administrateur ne connaissant jamais le mot de passe. Politique de mot de passe unifiée et relevée (10 caractères, majuscule/minuscule/chiffre) contre un simple `min(8)` auparavant. **Écran d'accueil de la régie créé** (`/moniteur`) : l'outil s'ouvrait sur le formulaire de grille alors qu'une régie a besoin d'un moniteur — ce qui est à l'antenne avec décompte du temps restant, programmes suivants, audience en direct ventilée web/mobile, et les quatre choses qui peuvent mal tourner (trou de grille, créneaux jamais répercutés, contrat expirant, absence de contenu de repli). Aucune règle métier ajoutée, uniquement l'agrégation de services existants. **Volet design** : ajout d'un `:focus-visible` global (la navigation clavier était invisible sur fond navy), d'un bloc `prefers-reduced-motion` (le badge EN DIRECT clignotait en boucle infinie devant un opérateur huit heures par jour), relèvement du contraste du texte tertiaire (≈3,4:1 sur `--color-surface`, sous le seuil AA, et appliqué justement aux plus petits textes), système de toasts avec `aria-live` remplaçant des bandeaux d'erreur invisibles en bas de tableau, dialogue de confirmation maison à la place de `window.confirm()` qui cassait le thème sombre, `role="dialog"` + piège à focus + fermeture par Échap + restitution du focus sur la modale. **Premiers tests automatisés du portefeuille** : 21 tests via `node:test` (intégré à Node 22, zéro dépendance ajoutée) sur les deux mécanismes dont une régression silencieuse coûterait le plus cher — détection des trous de grille (algorithme extrait en fonction pure) et garde de non-chevauchement (sur base réelle, plage 2030, auto-nettoyée). **Seed corrigé sur deux défauts trouvés en vérifiant** : il fabriquait un `DiffusionLog` à 480 vues sans aucune session derrière (exactement l'illusion à éviter en démonstration client — remplacé par de vraies sessions d'audience dont le log se déduit), et son créneau « en cours » ne durait que 30 minutes, laissant la démonstration hors antenne une demi-heure après le seed. **Vérifié en conditions réelles** : `tsc --noEmit` propre sur les trois bases (backend, frontend, mobile), 21/21 tests, 16/16 vérifications API sur serveur réellement démarré (cycle audience complet, RBAC 403 par rôle, idempotence de la génération), 27/27 vérifications en navigateur réel via Playwright (heartbeat émis par le player public et audience remontée jusqu'au moniteur de la régie, journal tracant réellement les actions, lien de réinitialisation généré puis consommé avec refus d'un mot de passe faible, sidebar filtrée par rôle, dialogue fermé par Échap, aucune erreur console). Trois défauts d'affichage corrigés après lecture des captures d'écran (ordre de la liste « à suivre » incompréhensible sans indication du jour, durées en minutes illisibles au-delà de l'heure, cartes de statistiques déséquilibrées). Base remise à l'état seed et serveurs arrêtés par PID en fin de session. **Reste hors périmètre, documenté dans le README** : alerte sortante (email/webhook) sur trou d'antenne — le moniteur ne prévient que si quelqu'un l'ouvre —, Docker/CI, couverture de test au-delà des deux mécanismes critiques. Jamais commité en git au moment de l'écriture de cette entrée.
- **ASSOCOTISE** : gestion financière associative (membres cotisants, cotisations mensuelles, dépenses, tableau de bord et rapports). Stack : React 19 + Vite + TypeScript + Tailwind 4 + Firebase (Firestore, Auth, Storage), **premier projet du portefeuille sans backend applicatif** (100 % client). **Quatre rôles depuis le 2026-07-28** (initialement deux) : secrétaire (membres et saisie des cotisations), trésorière (cotisations + dépenses, sans administration), coordonnateur (accès complet, seul à administrer : comptes, paramètres, clôture d'exercice — stocké en base sous la valeur historique `responsable_finances` pour ne pas casser le compte déjà déployé en prod, seul le libellé UI a changé) et membre du comité (lecture seule : dashboard, cotisations, dépenses, rapports — aucune écriture nulle part). Cotisations **append-only** : une correction crée toujours un nouveau document, jamais une réécriture, seul le champ `annulee` est modifiable et uniquement par le coordonnateur ou la trésorière, aucune suppression n'est autorisée nulle part (désactivation par statut). Thème clair/sombre, génération de rapport PDF (react-pdf) et export CSV, Select personnalisé Headless UI. Le module tontine initialement prévu a été retiré. Découvert non documenté en session le 2026-07-21 (2 commits, aucune trace dans CONTEXT.md ni HISTORY.md). Sécurité entièrement portée par `firestore.rules`/`storage.rules` (pas de backend) : immuabilité de l'historique, exercices comptables verrouillables, anti auto-verrouillage responsable.

**Déployé en production le 2026-07-21 (même session)** : projet Firebase réel créé de zéro (`assocotise-dev`, Firestore région `nam5`, Auth Email/Password), règles et index Firestore déployés, Hosting sur https://assocotise-dev.web.app. Premier compte responsable finances créé via `creer-responsable.ts` (clé de compte de service générée par Jaslin dans la console, supprimée du disque après usage). **Storage délibérément non activé, décision budgétaire confirmée** : Firebase exige le plan Blaze (payant à l'usage) pour Storage depuis oct. 2024 ; Jaslin a explicitement confirmé ne pas pouvoir payer pour l'activer maintenant (contrainte financière assumée, pas un simple report). L'upload de justificatifs de dépenses reste donc indisponible — ne pas re-proposer d'activer Storage tant qu'il n'en reparle pas lui-même. Le reste (membres, cotisations, dashboard, rapports, exercices) vérifié fonctionnel en conditions réelles (connexion + dashboard chargé sur les vraies données Firestore de prod, aucune erreur console) — et Jaslin s'est lui-même connecté juste après pour ajouter un premier membre réel et une cotisation, confirmant l'usage réel de l'app déployée. CLI Firebase authentifié en OAuth utilisateur (`jslnoccius@gmail.com`, `firebase login` lancé par Jaslin lui-même dans un terminal séparé — le login interactif est irréalisable depuis les outils Bash/PowerShell de la session, non-interactifs par construction).

  **Audit et 4 chantiers livrés le 2026-07-21** (détail dans `context/HISTORY.md`) : (1) **règles Firestore durcies**, point central du projet puisque, sans backend, tout détenteur d'un compte du bureau peut écrire directement via le SDK sans passer par les formulaires React. Les règles ne validaient auparavant aucune donnée (montant négatif, mois mal formé, `memberId` inexistant, champ arbitraire injecté : tout passait, seul le formulaire React contrôlait). Ajout de la validation complète (types, formats, listes de champs bornées, existence réelle du membre référencé), immuabilité de `saisiPar`/`saisiLe` sur les dépenses avec `modifieLe` obligatoire pour toute modification de fond, garde anti auto-verrouillage (un responsable ne peut plus changer son propre rôle ni se désactiver). (2) **Paramètres de l'association** : `MONTANT_MINIMUM = 500` était codé en dur dans 5 fichiers, l'application ne pouvait servir qu'une seule association à 500 HTG. Nom, montant de cotisation et devise sont désormais dans `settings/association`, diffusés par un `ParametresContext` et repris partout, y compris le PDF et le CSV. (3) **Mots de passe et amorçage** : lien « Mot de passe oublié » (message neutre ne révélant pas quels emails existent), bouton de réinitialisation côté responsable, et script `npm run creer-responsable` créant le premier compte, impossible autrement puisque les règles exigent d'être déjà responsable finances pour créer un compte. Hosting Firebase configuré avec réécriture SPA. (4) **Module de relances** : page listant les membres actifs en retard avec le reste à payer, message paramétrable (variables `{nom}`, `{mois}`, `{montant}`, `{association}`), ouverture de WhatsApp (`wa.me`, avec normalisation des numéros et indicatif pays configurable) ou du client mail, et journal `reminders` append-only. Décision assumée : l'application n'envoie rien elle-même (aucun backend, aucun crédit SMS, pas d'API WhatsApp Business), la trace atteste de la démarche et non de la réception, ce qui est écrit explicitement dans l'interface. **5 bugs corrigés en chemin** : `npm run build` était déjà cassé avant la session (constante inutilisée), aucun `label` du design system n'était rattaché à son champ (`Field.tsx` corrigé avec `useId`), la fiche membre chargeait ses cotisations une seule fois au lieu d'écouter en temps réel, un membre inactif était affiché « En retard », et le titre du header ne suivait pas les nouvelles pages. Comptes de démonstration : secretaire@assocotise.ht / Secretaire123!, responsable@assocotise.ht / Responsable123!.

  **Seconde passe le même jour, sur « faire le reste » (2026-07-21) :** (5) **journal d'audit** consultable (page `/journal`, filtres par action et par auteur, export CSV), **dérivé des traces portées par les documents eux-mêmes** et non d'une collection de log séparée. Décision assumée : sans backend, un log écrit par le client pourrait tout simplement ne pas être écrit par qui voudrait dissimuler une action, ce qui donnerait une fausse garantie ; le dériver des documents le rend exactement aussi fiable que les données qu'il décrit. Ajout au passage de `annuleePar`/`annuleeLe` sur cotisations et dépenses, une annulation ne portant auparavant aucune trace (impossible de savoir qui avait annulé quoi). (6) **Clôture d'exercice annuel** (`exercices/{annee}`) : fige l'année, calcule le solde final et le reporte sur l'exercice suivant, réouverture possible et tracée. Le verrouillage est appliqué **par les règles Firestore** (fonction `exerciceOuvert` appelée sur chaque création, correction et annulation), pas seulement par l'interface. (7) **Rapports par période** (page `/rapports`) : presets et période personnalisée, taux de recouvrement, répartition des dépenses par catégorie, export CSV détaillé ligne par ligne des cotisations et des dépenses, PDF de la période. (8) **Bornage des écoutes du tableau de bord**, qui lisait toute la collection depuis l'origine : il lit désormais l'exercice en cours plus les 6 derniers mois et ajoute le solde reporté, les totaux « toutes périodes » devenant « exercice » (qui est aussi la lecture comptable juste). (9) **29 tests automatisés des règles** (Vitest + `@firebase/rules-unit-testing`, `npm test`), dont il a été vérifié qu'ils mordent réellement : une régression volontaire sur la validation des montants fait tomber 2 tests, qui repassent après restauration. (10) **PWA installable et hors-ligne** : manifeste, icônes générées, service worker (`vite-plugin-pwa`) et surtout cache Firestore persistant IndexedDB multi-onglets, plus un bandeau explicite en cas de perte de réseau pour que personne ne croie ses saisies déjà parties. **Performance** : le bundle faisait 2,8 Mo d'un bloc, ce qui bloquait aussi le précache du service worker ; moteur PDF et écrans admin chargés à la demande, bundle initial ramené à 347 kB (113 kB gzip), gain concret pour une connexion haïtienne. **Deux défauts de template jamais corrigés trouvés** : `index.html` était resté en `lang="en"` avec le titre « frontend », et le favicon était celui de Vite.

  **État réel : jamais déployé.** L'application n'a jamais tourné ailleurs que sur les émulateurs Firebase, `.firebaserc` pointe sur un projet factice `assocotise-dev` et `.env.local` est en `VITE_USE_EMULATOR=true`. **Créer le vrai projet Firebase est le seul bloquant restant** et demande les credentials de Jaslin, la procédure complète étant écrite dans le README (activation Auth/Firestore/Storage, déploiement des règles et index, `npm run creer-responsable`, puis hosting). **Reste à faire** : portail membre (seuls les comptes du bureau ont un accès, un cotisant ne peut pas consulter sa propre situation), tests de l'interface (seules les règles sont couvertes automatiquement), pagination du journal d'audit (limité aux 200 entrées les plus récentes par type de donnée).
- **SMART GROS-MORNE** : portail vitrine de la commune de Gros-Morne (Artibonite, Haïti), commandé par un client réel, 20 sections (accueil, découvrir/histoire/culture/patrimoine, tourisme, économie, investir, annuaire, actualités, agenda, galerie, diaspora, services municipaux, vie associative, éducation, santé, contact, à propos, FAQ, pages légales), tout le contenu devant être administrable depuis un dashboard sans intervention développeur. Un frontend Next.js 16 + Tailwind 4 + Radix UI + Framer Motion existait déjà (`livrables/applications/gros-morne/`), jamais documenté au moment de sa découverte le 2026-07-28 — et, découvert plus tard dans la même session, en réalité déjà committé (noyé sans mention dans un commit `16b4eb4` au message consacré à BANKA, probablement via un `git add -A` global) : 9 pages statiques (accueil, histoire, géographie, culture, personnalités, tourisme, communauté, galerie, actualités), contenu en dur, aucun backend/base/admin. Cahier des charges complet reçu le 2026-07-28 et sauvegardé dans `PLAN.md` à la racine du projet avec un phasage proposé (Phase 0 fondations backend/admin, Phase 1 Accueil+Découvrir, Phase 2 Tourisme+Annuaire, Phase 3 Actualités+Agenda+Galerie, Phase 4 sections à structure "fiche" restantes, Phase 5 pages statiques/SEO/perf). Décisions prises avec Jaslin : on continue sur l'existant plutôt que repartir de zéro, backend maison Express+Prisma+PostgreSQL (cohérent avec le reste du portefeuille) plutôt qu'un CMS headless tiers, langues FR+Kreyòl dès le départ.

**Phase 0 livrée et vérifiée le 2026-07-28.** Réorganisation structurelle du projet (jamais commité, donc sans risque) : le frontend existant déplacé de la racine vers `frontend/`, nouveau `backend/` créé à côté — aligne enfin gros-morne sur la convention backend/frontend du reste du portefeuille. Stack et conventions reprises à l'identique du reste du portefeuille (layout modules par domaine façon POSTA/OTELA, JWT access 15 min + refresh 30j en cookie httpOnly haché SHA-256 en base, `validate.middleware.ts` réassignant bien body/query/params, upload disque local façon REYINYON hors de `backend/src`). Ports attribués : 4010 (backend) / 3010 (frontend). Modèle Prisma : `AdminUser`/`RefreshToken` (auth), `Media` (upload images/PDF 10 Mo max), et surtout `PageSection`/`PageSectionTraduction` — le mécanisme de contenu générique et multilingue promis par le cahier des charges (un bloc de texte identifié par `page`+`cle`, une traduction par locale FR/HT), pensé pour que les Phases 1-5 y branchent tout texte libre (hero, mots de bienvenue, pages légales...) sans nouvelle table à chaque fois ; les entités répétitives à venir (Tourisme, Annuaire, Actualités...) suivront le même schéma de traduction mais avec leurs propres modèles dédiés. Dashboard admin minimal mais fonctionnel de bout en bout : `/admin/login`, `/admin` (accueil), `/admin/contenu` (CRUD complet du mécanisme générique avec formulaire bilingue FR/HT), design system porté depuis GESCOM/ANTENN (Badge/EmptyState/Modal/PageToolbar/StatCard) et scopé sous une classe `.gm-admin` pour ne jamais toucher au CSS du site public existant. **Vérifié en conditions réelles** : API (curl — login, RBAC 401 sans token, création/lecture publique/mise à jour/suppression d'un bloc bilingue, upload d'image servi statiquement) et navigateur (Playwright, 8/8 étapes : redirection non connecté, login réel, création/persistance après rechargement/édition/suppression d'un bloc de contenu, non-régression des pages publiques `/` et `/histoire`). `tsc --noEmit` propre backend et frontend. Un bug de dépendance trouvé et corrigé en cours de route : `multer` mis à jour en 2.2.0 (1.x listé avec des CVE connues par npm) puis `@types/multer` réinstallé après avoir découvert que multer 2.x n'embarque aucune déclaration TypeScript (contrairement à l'hypothèse initiale), qui avait cassé la compilation. Vulnérabilités npm résiduelles côté frontend (next/postcss/sharp/js-yaml/brace-expansion, `npm audit fix --force` proposerait un bump vers next 16.2.12) laissées **volontairement non corrigées** dans cette session : préexistantes au projet hérité, sans lien avec le travail de la Phase 0, et une montée de version de Next mérite son propre test plutôt qu'un correctif improvisé en fin de session — signalé à Jaslin, pas encore traité. Base remise à un état de seed propre (un seul compte admin, aucune donnée de test) après vérification, serveurs arrêtés. Comptes seed : `admin@grosmorne.ht` / `GrosMorne@123`.

**Phase 1 élargie livrée et vérifiée le 2026-07-28 (même jour)**, sur demande explicite de Jaslin qui a fourni deux maquettes de référence (style civique/institutionnel : top-bar, méga-menu, hero à carte de statistiques flottante, footer 5 colonnes) et demandé d'ajouter toutes les sections du cahier des charges d'un coup plutôt que de suivre le phasage fin initialement proposé. Refonte visuelle : `TopBar` (contact + réseaux sociaux), `Navbar` en méga-menu (dropdowns « Découvrir Gros-Morne » et « Tourisme », sélecteur de langue FR/HT encore statique, recherche non branchée), `HeroSection` réécrit (image + carte de statistiques flottante, ancien `StatsBar` supprimé car intégré au hero), `Newsletter` (nouveau composant), `Footer` réécrit (5 colonnes + liens légaux). **14 pages construites** pour couvrir l'intégralité des 20 sections du cahier des charges : Annuaire (recherche + filtres par catégorie fonctionnels côté client), Agenda (mini-calendrier + liste d'événements), Investir, Diaspora, Contact (formulaire), À propos, FAQ (accordéon Radix), Services municipaux, Vie associative, Santé, Éducation, Économie (ces 4 dernières via un composant `FicheCardGrid` partagé pour éviter la duplication), et les 3 pages légales (mentions légales, confidentialité, conditions d'utilisation) via un composant `LegalContent` partagé. **Toutes ces pages restent statiques** (mêmes tableaux de données en dur que les 9 pages déjà existantes) — pas encore branchées sur le mécanisme `PageSection` de la Phase 0, ce câblage étant repoussé à une phase suivante plutôt que fait à la hâte.

**Un vrai bug de compatibilité trouvé et corrigé** : la version de `lucide-react` installée (1.21.0, plus récente que ce que Claude connaît) n'exporte plus aucune icône de marque (`Facebook`/`Instagram`/`Youtube`/`Twitter` inexistants), cassant la compilation TypeScript du nouveau `TopBar`/`Footer` — corrigé en réutilisant le contournement déjà présent dans l'ancien `Footer` du projet (icônes génériques `Share2`/`MessageCircle`/`Camera`/`Globe` à la place des logos de marque). **Incident opérationnel sans rapport avec le code** : un processus `next dev -p 3010` zombie (survivant à un arrêt de tâche précédent, `TaskStop` ne tuant pas toujours le process enfant `next` sur Windows) bloquait le port 3010 au redémarrage — identifié via `wmic` et tué manuellement (`taskkill`).

**Vérifié en conditions réelles** : `tsc --noEmit` propre, puis Playwright (headless) sur les 24 pages du site (9 existantes + 14 nouvelles + accueil) — 200 partout, aucune erreur console/page/HTTP — plus les interactions réelles (méga-menu au survol puis navigation, accordéon FAQ, filtre catégorie de l'Annuaire, soumission du formulaire de contact), et captures d'écran desktop/mobile confirmant le rendu (29/29 vérifications OK). Reste à faire, documenté et assumé comme tel : câblage backend/admin section par section (Phase 2+ révisée), recherche et carte interactive réelles, sélecteur de langue FR/HT non fonctionnel, contenu de ces 14 nouvelles pages toujours provisoire/représentatif (à valider avec Jaslin avant mise en production).

**Cahier des charges enrichi + re-phasage + Phase 2a (2026-07-28, même jour, suite).** Jaslin a fourni une version très détaillée (20 sections + 28 modules admin + ~35 tables proposées). Audit confirmant qu'aucune des entités métier proposées n'existait encore (la Phase 1 ci-dessus n'avait livré que du frontend statique). Décisions actées avec Jaslin : rôles en **enum Prisma fixe** (pas de moteur RBAC générique), réconciliation des doublons du document (un modèle dédié par vertical ayant son propre module admin, `AnnuaireEntry` générique en catch-all, sponsors+partenaires fusionnés en un seul modèle `Partenaire`). Phasage réactualisé (2a-2f/3/4/5) écrit dans `livrables/applications/gros-morne/PLAN.md`. **Phase 2a livrée le jour même** (choix de Jaslin parmi 4 options) : modèles `SectionCommunale`/`Personnalite` (+ traductions FR/HT), écrans admin dédiés, `SectionsCommunes.tsx` (accueil) et `PersonnalitesSection.tsx` (`/personnalites`) désormais branchés sur l'API réelle au lieu du contenu en dur. Les 8 sections + 4 personnalités réelles insérées en base. Détail complet (dont un incident d'encodage UTF-8 côté shell de vérification, corrigé) dans `context/HISTORY.md`.

**Phase 2b — Tourisme livrée (2026-07-28, même jour, suite).** Modèles `TourismPlace`/`TourismPlaceTraduction` (+ `TourismPlacePhoto`, table de jonction vers le `Media` déjà existant) et deux nouveaux enums `CategorieTourisme`/`StatutPublication` (ce dernier réutilisable par Actualités/Événements en Phase 2c). Deux endpoints (`GET /tourisme` public ne renvoyant jamais un brouillon, `GET /tourisme/admin` protégé voit tout). Écran admin `/admin/tourisme` avec sélecteur de photos réel (upload ou réutilisation depuis la bibliothèque média). `TourismeSection.tsx` (`/tourisme`) branché sur l'API. **Les 6 fiches déjà en ligne migrées telles quelles** (Mornes Panoramiques, Sources & Rivières, Forêts, Centre-Ville Historique, Marchés Locaux, Fête Patronale) — décision assumée de ne pas utiliser les noms de lieux très spécifiques visibles sur les captures fournies par Jaslin, non vérifiables comme réels pour la commune. Détail complet dans `context/HISTORY.md`.

**Phase 2c — Actualités & Agenda livrée (2026-07-28, même jour, suite).** Modèles `Article`/`Evenement` (+ traductions FR/HT, image principale via `Media`), enums `CategorieArticle`/`CategorieEvenement`. Agenda public filtré aux événements publiés **et** à venir (date UTC ≥ aujourd'hui, testé explicitement). Écrans admin `/admin/actualites` et `/admin/agenda`. **Bug réel trouvé et corrigé** : décalage d'un jour à l'affichage des dates (fuseau local appliqué à une date stockée en UTC minuit) — corrigé en forçant `timeZone: "UTC"`/méthodes `getUTC*` partout où une date est affichée. Les 6 articles réels migrés avec leurs dates historiques exactes (statut publié) ; les 5 événements réels migrés en **brouillon** plutôt que publiés, leurs dates dans le code hérité n'ayant aucune année fiable — laissés à la charge de Jaslin pour un vrai calendrier avant publication. Détail complet dans `context/HISTORY.md`.

**Phase 2d — Galerie & Vidéos livrée (2026-07-29).** `GalerieAlbum`/`GalerieMedia` (média nullable + `icone` emoji en repli — le contenu hérité n'a jamais eu de vraies photos, seulement des emoji de substitution) et `Video` en module admin séparé (lien externe YouTube/Vimeo). 5 albums thématiques créés (Nature/Vie locale/Événements/Architecture/Culture) reprenant les 8 "photos" déjà en ligne telles quelles ; compteur de "likes" fictif retiré (aucune donnée d'engagement réelle) ; aucune vidéo migrée ou inventée (fonctionnalité entièrement nouvelle, base vide). `GalerieSection.tsx` et l'aperçu `GalleryPreview.tsx` de l'accueil (hors périmètre strict mais dupliquait les mêmes fausses données) câblés sur l'API. Détail complet dans `context/HISTORY.md`.

**Phase 2e — Répertoires métier livrée (2026-07-29, suite).** Plus gros chantier de la Phase 2 : 8 modèles Prisma (`Business`, `Hotel`, `Restaurant`, `School`, `HealthFacility`, `Association`, `MunicipalService`, `AnnuaireEntry` catch-all), 8 modules backend, 8 écrans admin. `/annuaire` agrège désormais 7 des 8 modèles (`MunicipalService` exclu, hors du cahier des charges pour cette page) tout en gardant le filtre client existant. **Économie et Investir laissés statiques** — leur contenu hérité est une prose de secteurs économiques, pas une liste d'entités nommées, hors du périmètre "répertoire". **Déduplication réelle** : 3 entités qui existaient à la fois sur leur page dédiée et dans l'ancien Annuaire statique (Lycée National, Hôpital Saint-Antoine, Association des Femmes Unies) migrées une seule fois. 4 entités (2 entreprises, 1 hôtel, 1 restaurant) qui n'existaient que dans l'Annuaire sans description ont reçu une phrase générique non-factuelle pour satisfaire le champ obligatoire. `AnnuaireEntry` reste vide (aucune vraie fiche banque/église/ONG dans le contenu hérité). Détail complet dans `context/HISTORY.md`. Prochaine étape : Phase 3 (recherche/filtres serveur réels, carte interactive).

**Correctif hors-phasage — images de fond des héros (2026-07-29, suite).** Retour de Jaslin : "chaque héro doit avoir une image de fond". Diagnostic : le hero accueil faisait tourner un faux carrousel de 3 fois la même photo Wikipedia d'une cascade de **Ville Bonheur** (autre commune, factuellement fausse pour Gros-Morne), les 23 autres pages n'avaient soit aucune image soit la même photo en dur. Décisions de Jaslin : gérable depuis l'admin (pas codé en dur), et c'est lui qui uploade ses propres vraies photos (pas de photo de stock de remplacement). Nouveau modèle `PageHeroImage` (clé `page` parmi 24 valeurs, relation vers `Media` déjà existant), module backend dédié, composant `DynamicPageHeader` câblé sur les 23 pages secondaires + réécriture du hero accueil, écran admin `/admin/images-hero`. Photo Wikipedia incorrecte retirée de ses deux emplacements (hero Tourisme, bloc décoratif Investir), remplacée par un gradient neutre en attendant. **Vérifié en conditions réelles** : Jaslin a lui-même uploadé 3 vraies photos pendant la session (accueil/histoire/géographie), chacune confirmée affichée sur sa propre page ; page sans upload confirmée en repli gradient neutre. Détail complet dans `context/HISTORY.md`.

**Fiches de détail + refonte accueil/Histoire (hors-phasage, 2026-08-02)**, sur maquettes fournies par Jaslin. Modèle `Personnalite` enrichi (catégorie fixe, informations biographiques, réalisations, citation) avec fiche de détail dédiée `/personnalites/[id]` et `/actualites/[id]` (jusque-là aucune page de détail n'existait, les clics sur les articles/personnalités ne menaient nulle part). Accueil reconstruit pour matcher la maquette (Découvrir/Stats en direct/Lieux incontournables/Actualités+Agenda/Investir/Partenaires), anciennes sections datant d'avant la Phase 1 supprimées (dont un module de dons fictifs avec montants inventés). Page Histoire refaite avec sommaire latéral + cartes de périodes + frise, Footer passé à 5 colonnes, bouton Navbar « Rejoindre » remplacé par « Espace admin ». **Reste ouvert** : logo réel de la commune non intégré (fichier non récupérable depuis le chat), incohérence préexistante de la population affichée selon les pages (132 245 vs ~155 692) jamais réconciliée. Détail complet dans `context/HISTORY.md`.

**Phase 2f — Pages transverses & configuration livrée (2026-08-06) : Phase 2 (2a-2f) entièrement close.** 6 nouveaux modèles : `Temoignage` (nouvelle section homepage, masquée tant que vide — aucun contenu hérité à migrer), `Partenaire` (fusion sponsors+partenaires, emplacements `ACCUEIL`/`A_PROPOS`), `MessageContact` (formulaire public `/contact`, aucune authentification requise pour écrire), `AbonneNewsletter` (upsert idempotent, export CSV, pas d'envoi de campagne), `Faq`, `SiteSettings` (singleton, alimente Footer/TopBar/Contact). **Journal d'activité (`ActivityLog`) livré via un seul middleware générique** plutôt qu'un retrofit des ~20 modules existants — capture automatiquement toute requête admin mutante réussie en dérivant l'entité de `req.originalUrl`, vérifié fonctionnel sur des actions hors phase sans configuration par module. `TopBar.tsx` corrigé au passage (même défaut d'icônes réseaux sociaux mortes que l'ancien Footer). Contenu hérité réel migré (6 FAQ, 2 vrais partenaires) ; les 5 noms d'institutions non vérifiées sur `AProposSection` (signalés le 2026-08-02) retirés plutôt que migrés. Incident détecté et corrigé en vérification : un script bash de migration a corrompu l'encodage UTF-8 des accents, recréé proprement via Node.js. **Phase 3 — Recherche, filtres réels et carte interactive livrée le même jour (2026-08-06, suite).** Audit préalable révélant que Tourisme avait déjà (non documenté) recherche serveur + carte Leaflet réelle, et que l'Annuaire avait déjà une carte + recherche mais **entièrement côté client** (7 verticaux chargés en entier puis filtrés en mémoire navigateur). Livré : recherche serveur ajoutée à Actualités et Agenda (mêmes conventions que Tourisme) ; nouvel endpoint agrégateur `GET /annuaire/toutes?secteur=&q=` remplaçant le filtrage client par une vraie recherche Postgres à travers les 7 verticaux, libellés de catégorie centralisés côté serveur. Bug latent découvert (École/Association référençaient un champ GPS inexistant en base) corrigé au passage. **Ceci clôt la Phase 3.**

**Phase 5 (volet SEO technique) livrée le même jour (2026-08-06, suite 2), sur "vas y" répété sans réponse à la clarification demandée** — décision assumée de traiter uniquement le SEO technique (pas d'identifiants externes ni de contenu Kreyòl requis, contrairement à la newsletter SMTP/sécurité avancée de Phase 4 ou à l'i18n réel du reste de la Phase 5). Livré : `robots.ts`/`sitemap.ts` (24 pages + articles/personnalités publiés), Open Graph/Twitter Card par défaut sur `layout.tsx` (avec correction d'une erreur factuelle préexistante : "Nord-Ouest d'Haïti" → Artibonite), `generateMetadata` dynamique sur les fiches article/personnalité (chaque fiche partageait auparavant le même titre générique), JSON-LD `GovernmentOrganization` sur l'accueil. **Phase 4 (volet envoi réel de newsletter) livrée le même jour (2026-08-06, suite 3)**, sur un troisième "vas y" sans réponse à la clarification — décision assumée de traiter le seul item constructible sans identifiant externe manquant (2FA aurait changé l'UX de connexion sans être demandé, CAPTCHA exige une clé tierce). `utils/mailer.ts` (nodemailer) avec repli gracieux si SMTP non configuré (contenu journalisé, jamais perdu, même philosophie que POSTA), `POST /api/newsletter/envoyer` (pas de modèle "Campagne" persisté, fidèle à la limite posée en Phase 2f), écran admin enrichi. Incident opérationnel rencontré et corrigé : plusieurs instances backend zombies accumulées dans la journée (7 processus Node trouvés), toutes arrêtées.

**Phase 4 (bibliothèque de fichiers générique) livrée le 2026-08-07**, sur un quatrième "vas y" sans réponse — nouveau modèle `Document` réutilisant le mécanisme Media déjà en place (upload PDF déjà autorisé), page publique `/documents` + écran admin. Deux incidents opérationnels rencontrés et résolus (processus `prisma migrate dev` zombies bloquant la migration ; charge CPU élevée ralentissant le démarrage du backend, due à l'accumulation d'instances `npm run dev` non nettoyées + un projet Vite sans rapport tournant en parallèle).

**Reste : i18n réel** (sélecteur HT toujours décoratif), **performance**, et le reste de la Phase 4 (commentaires/modération, notifications, menus dynamiques, sauvegardes UI, 2FA/CAPTCHA/liste noire IP), non traités faute de décision explicite de Jaslin sur la priorité. Détail complet dans `context/HISTORY.md`.
- **OTELA** : PMS hôtelier multi-établissements (réservation en ligne + back-office réception/établissement/chaîne) pour un client Haitech Solutions, à partir de deux prompts de spécification détaillés (cœur PMS en 4 phases + extension "5 étoiles" avec folio/restaurant/spa/événementiel en 9 modules). Construit de zéro le 2026-07-09, sur "VAS Y" : ampleur totale des deux documents jugée supérieure à tout projet livré en une session jusqu'ici, scope volontairement limité à la **Phase 1 du cœur PMS** (modèle de données, moteur anti-double-booking, site public de réservation, back-office établissement, back-office chaîne) après plan validé (EnterPlanMode) ; Phases 2-4 et l'intégralité de l'extension 5 étoiles documentées comme prochaines étapes, pas codées. Stack : Express 4 + TypeScript + Prisma v5 + PostgreSQL (backend, port 4007) + Next.js App Router (frontend, port 3008), conventions reprises du reste du portefeuille (RBAC hiérarchique LAKAY, multi-tenant `resolveEtablissement` calqué sur `resolveBoutique` de SHOPAY, auth cookie httpOnly + verrou de refresh partagé appliqué préventivement dès le départ comme sur ANTENN, design system Badge/StatCard/PageToolbar/EmptyState/Modal de GESCOM/ANTENN avec palette dédiée navy/or). Modèle : Chaine, Etablissement (devises acceptées par tableau Postgres natif), TypeChambre, Chambre, Tarif (par devise HTG/USD, jamais de conversion automatique, snapshotté sur la réservation au moment de la réservation), Client (profil unique multi-établissements dédupliqué par email), Reservation (statut confirmee/en_attente/annulee/terminee/no_show, champ `canal` préparé pour la Phase 4 channel manager sans être utilisé), Employe (4 rôles : reception, menage, administrateur_etablissement, administrateur_chaine). **Moteur anti-double-booking à deux niveaux**, le point le plus critique du cahier des charges : contrainte d'exclusion PostgreSQL (`EXCLUDE USING gist` sur `chambreId` + `tsrange(dateArrivee, dateDepart)`, extension `btree_gist`, migration SQL manuelle écrite à la main comme les vues `mail_domains` de POSTA puisque Prisma ne sait pas exprimer une contrainte d'exclusion) doublée d'un pré-check applicatif (retour rapide, message clair) dans un service unique `reservations.service.ts::creerReservation()` utilisé à l'identique par le site public et par la création manuelle back-office. **Deux bugs réels trouvés et corrigés en vérification** (pas de simple lecture de code) : `GET /chambres/types` était laissée accessible sans authentification pour un usage "public" qui n'existait finalement pas côté frontend (le site public passe par `/disponibilite`), cassant le chargement du back-office établissement faute d'`etablissementId` résolu, corrigé en la réservant au personnel authentifié ; et surtout `validate.middleware.ts` (pattern copié tel quel du reste du portefeuille) validait les payloads avec Zod sans jamais réassigner le résultat parsé à `req.body`/`req.query`/`req.params` — un `z.coerce.date()` ne produisait donc aucun effet concret et la création de tarif plantait en 500 côté Prisma avec une chaîne brute au lieu d'une date ; corrigé par réassignation conditionnelle (seulement les clés effectivement déclarées par chaque schéma, pour ne pas écraser les `params`/`query` non validés des routes qui n'en définissent pas). **Vérifié en conditions réelles** : API (curl) avec un vrai test de concurrence (8 requêtes simultanées sur exactement la même chambre/dates, une seule a réussi, les 7 autres 409 "Chambre non disponible sur ces dates"), garde d'immutabilité, double-annulation refusée, isolation stricte par établissement, création de type de chambre + tarif + établissement, RBAC 403 sur une action réservée à la chaîne ; navigateur (Playwright, Chromium installé à la volée dans le scratchpad après un échec de téléchargement du build headless_shell contourné en pointant directement sur le Chromium complet) couvrant le parcours public complet (recherche → réservation → confirmation), connexion + navigation des 4 pages back-office établissement, création manuelle de réservation via le vrai formulaire, annulation depuis l'interface, connexion admin chaîne avec séparation RBAC visuelle confirmée (aucun lien Chambres/Réservations dans son menu) et vue consolidée sur les 2 établissements. `tsc --noEmit` propre backend et frontend. Base de données remise à un état de seed propre après vérification (`prisma migrate reset` + reseed). Comptes seed : reception@otela.ht / administrateur@otela.ht / chaine@otela.ht, mot de passe Otela@123.

**Phase 2 — réception et ménage (2026-07-09, "on continue")** : `Chambre.statut` étendu à OCCUPEE/NETTOYAGE_EN_COURS (en plus de DISPONIBLE/MAINTENANCE), nouveau modèle `TacheMenage`. Décision de conception explicitée dans le plan : OCCUPEE (séjour en cours) ne bloque jamais une recherche de disponibilité sur des dates futures (le chevauchement de réservation suffit déjà), alors que NETTOYAGE_EN_COURS bloque la chambre sur **toutes** les dates tant que le ménage n'est pas marqué terminé, conformément au texte exact du document ("ne doit jamais apparaître comme disponible... tant que le ménage n'est pas marqué terminé") — vérifié une première fois avec la mauvaise attente (croyant que NETTOYAGE_EN_COURS devait aussi épargner le futur), corrigé après relecture du document plutôt que de changer le code. Modules `reception/` (vue du jour, check-in/check-out avec compare-and-swap sur `Chambre.statut`) et `menage/` (liste des tâches, progression A_FAIRE→EN_COURS→TERMINE, réassignation employé, remise automatique de la chambre en DISPONIBLE au statut TERMINE). **Bug réel trouvé et corrigé** (pas latent depuis la Phase 1, révélé seulement par le besoin Phase 2 de réserver "pour aujourd'hui même") : la vérification anti-date-passée et le calcul de la fenêtre "aujourd'hui" de la vue de réception comparaient une date ISO `YYYY-MM-DD` (parsée en minuit UTC) à un minuit calculé en heure **locale** serveur (America/Port-au-Prince, UTC-4) — décalage de 4h qui rejetait à tort toute réservation pour le jour même et faisait manquer les arrivées/départs du jour à la réception ; corrigé en calculant systématiquement le début de journée en UTC. **Second bug trouvé en vérification** : la réponse de l'API ménage exposait le hash du mot de passe de l'employé assigné (`include: { employeAssigne: true }` sans `select`) — corrigé avant tout test navigateur. Vérifié en API (check-in/check-out avec CAS, double check-in/check-out rejetés, tâche générée au check-out, ménage terminé restaure la disponibilité) et navigateur (RBAC visuel réception/ménage/admin établissement, clics réels sur les boutons Check-in/Check-out/Marquer).

**Module Gestion des Employés (2026-07-11/12) :** sur question de Jaslin ("qu'en est-il de la gestion des employés ?"), audit confirmant un vrai manque — `Employe` n'était qu'un compte de connexion, aucun écran ne permettait d'en créer un, seuls les comptes du seed existaient. Jaslin a validé deux chantiers à traiter l'un après l'autre (gestion des employés puis tarif day-use pour les clients ne réservant pas de nuit complète) ; celui-ci est le premier. Scope volontairement limité à un CRUD complet (pas un module RH façon BANKA avec contrats/paie/congés). Décisions de sécurité, cœur du module : mot de passe initial fixé par l'admin à la création (pas d'infrastructure de token d'invitation, disproportionnée pour ce seul besoin) ; anti-escalade de privilèges stricte (`ADMINISTRATEUR_ETABLISSEMENT` ne peut jamais créer/promouvoir un compte `ADMINISTRATEUR_CHAINE`, cloisonné à son propre établissement même si un `etablissementId` étranger est forcé dans la requête — testé et confirmé ignoré côté serveur) ; garde anti-auto-verrouillage (un admin ne peut pas désactiver son propre compte) ; réinitialisation de mot de passe par un admin distincte du changement de mot de passe classique (révoque les sessions actives de la cible, même pattern que `auth.service.ts::changePassword`). Page `/employes` unique pour les deux niveaux d'admin (sélecteur d'établissement + rôle admin chaîne disponibles uniquement pour `ADMINISTRATEUR_CHAINE`), reprend le pattern déjà établi de `/etablissements`. Vérifié en API (curl : création avec cloisonnement, refus 403 sur tentative de créer un compte chaîne par un admin établissement, refus cross-établissement, email dupliqué en 409 propre, auto-désactivation refusée, cycle complet de réinitialisation de mot de passe avec ancien mot de passe refusé et nouveau accepté, aucune réponse API n'expose jamais le champ `password`) et navigateur (Playwright : connexion admin établissement et admin chaîne, création réelle via formulaire, désactivation/réactivation, réinitialisation de mot de passe). **Incident de vérification (pas un bug produit)**, même pattern que Room Service tranche 4 : le script Playwright a rapporté 3 faux échecs (nouvel employé pas visible dans la liste juste après création, message de confirmation de reset absent, nouvel admin chaîne pas visible) à cause de lectures du texte de page trop précoces par rapport au re-render asynchrone du store Zustand — confirmé via API directe immédiatement après que les trois opérations avaient bien abouti (employés présents, mot de passe effectivement changé). `tsc --noEmit` propre backend et frontend. Base de données remise à un état de seed propre après vérification, serveurs arrêtés. Prochaine étape validée par Jaslin : tarif day-use (clients ne réservant pas une nuit complète), pas encore planifiée.
- **REYINYON** : application de visioconférence WebRTC (créole "réunion"), pensée pour la résilience en contexte de connexion instable (marché haïtien/Caraïbes), à partir d'un prompt de spécification détaillé fourni le 2026-07-10. Construit de zéro sur LiveKit (SFU open source) **réellement self-hébergé via Docker** dans cet environnement (contrairement à ErsatzTV/CDN sur ANTENN, jamais disponibles) — premier projet du portefeuille avec une vérification vidéo/audio bout-en-bout tentée en conditions quasi réelles plutôt que purement documentée. Stack : Express 4 + TypeScript + Prisma v5 + PostgreSQL (backend, port 4008) + Next.js App Router (frontend, port 3009), conventions reprises d'OTELA (auth JWT 15min + refresh cookie httpOnly + verrou de rafraîchissement partagé appliqué dès le départ, design system Badge/StatCard/PageToolbar/EmptyState/Modal, palette dédiée navy/vert menthe). Déviation assumée du RBAC habituel du portefeuille : permissions scopées PAR réunion (hôte = `reunion.hoteId`) plutôt qu'un rôle global sur le compte, un même utilisateur pouvant être hôte d'une réunion et simple participant d'une autre. Modèle : Reunion (code type Google Meet xxx-xxxx-xxx, code d'accès optionnel, code téléphone à 6 chiffres, salle d'attente, verrouillage), Participant (avec ou sans compte, `reconnectToken` opaque hashé stocké en sessionStorage — jamais localStorage, partagé entre tous les onglets d'un même navigateur et casserait l'isolation entre plusieurs "participants" testés depuis le même poste — permettant la reprise de session en un clic après coupure sans repasser par la salle d'attente), MessageChat (texte, photo ou message vocal — `type` TEXTE/PHOTO/AUDIO, upload local via multer 10 Mo max, servi via `/uploads/chat`, même principe que `/recordings`), Enregistrement (réellement câblé, cf. ci-dessous). Différenciateurs livrés : dégradation automatique vers audio seul visible (bandeau "X est passé en audio seul", pas un flou silencieux), mode "données minimales" choisi consciemment à la pré-jointe, indicateur de qualité réseau toujours visible par participant, invitation WhatsApp (texte prêt à partager + lien wa.me + numéro/code téléphone si dial-in configuré, pas d'automatisation d'envoi), sélecteur de langue FR/Kreyòl, notifications "X a rejoint/a quitté la réunion" (toasts éphémères, branchés directement sur les événements LiveKit), contrôles hôte micro et caméra séparés (un seul bouton coupait les deux au départ, corrigé sur demande explicite — hôte peut seulement couper, jamais rallumer à la place du participant, même limite que Zoom/Meet).

**Correctifs d'usage réel (session du 2026-07-11, remontés par Jaslin en testant lui-même)** : panneau d'admission de la salle d'attente invisible côté hôte sur un onglet neuf — `/rejoindre` et `/salle` sont hors du layout `(dashboard)` protégé, donc l'access token n'était jamais restauré depuis le cookie ; corrigé en appelant `hydrate()` directement sur ces deux pages. Départ volontaire ("Quitter") ne mettait jamais à jour la base (participant restait "Présent" indéfiniment) — nouvel endpoint self-service `POST /participants/:id/quitter`, idempotent. **Enregistrement (LiveKit Egress, session du 2026-07-11)** : self-hébergé réellement (sortie fichier local, pas besoin de S3/GCS), backend/frontend complets et vérifiés (permissions hôte, auto-guérison si Egress meurt tout seul plutôt que de rester bloqué "en cours" indéfiniment). Un vrai bug réseau conteneur-à-conteneur trouvé et corrigé (`network_mode: service:livekit` — même cause que le NAT hairpinning découvert plus tôt dans la session côté navigateur, mais côté conteneur Egress cette fois : `node_ip: 127.0.0.1` de `livekit.yaml` est un loopback propre à chaque conteneur, inutilisable par Egress pour joindre le flux WebRTC de LiveKit tant qu'ils ne partagent pas le même network namespace). **Non confirmé après un diagnostic approfondi (5 tentatives, ressources écartées — 8 CPU/15,5 Go alloués à Docker confirmés suffisants)** : la production effective du fichier vidéo. Chrome (embarqué dans l'image Egress, nécessaire pour composer le layout de la réunion) finit par démarrer réellement après un redémarrage complet de la stack Docker (processus actifs, ~1,2 Go de RAM utilisée confirmés), mais le signal "enregistrement démarré" n'arrive jamais, même avec un participant réellement connecté confirmé dans la salle au moment de la tentative. Cause probable restante : instabilité du sandbox de virtualisation imbriquée (Docker Desktop/WSL2) après une session très longue — un plantage runtime OCI de bas niveau sur le conteneur Egress a été observé juste avant ce diagnostic. À revérifier par Jaslin sur un vrai serveur avant de considérer cette fonctionnalité fiable.

**Dial-in téléphonique (session du 2026-07-11)** : Jaslin a un compte Twilio (numéro **américain**, pas local Haïti). IVR réel construit et vérifié côté serveur (`modules/telephony/`, signature Twilio validée, code réunion à 6 chiffres `Reunion.codeTelephone`), mais jamais testé avec un vrai appel (tunnel public refusé par Jaslin). Le pont audio final vers LiveKit (SIP, nécessite IP publique + TLS) reste documenté mais non codé — `integrations/telephony.ts`. **Bug réel trouvé et corrigé en vérification** : React StrictMode (dev) double-invoque l'effet de connexion LiveKit, et le cleanup du 1er montage déconnectait la room pendant que `connect()` du 2e montage était encore en cours, affichant une erreur par-dessus une connexion qui avait en fait réussi — corrigé par un garde d'annulation (`RoomProvider.tsx`). **Vérifié en conditions réelles** : API (curl — RBAC hôte/non-hôte, code d'accès erroné, réunion verrouillée 409, salle d'attente + admission avec émission du token LiveKit, chat REST), navigateur (connexion, création de réunion, écran de pré-jointe, reprise de session automatique confirmée fonctionnelle). **Deux bugs supplémentaires trouvés et corrigés grâce à un test réel de Jaslin sur sa propre machine** (le sandbox de vérification ne pouvait pas les révéler lui-même) : 1) `livekit.yaml` avec `use_external_ip: true` faisait découvrir à LiveKit son IP **publique** via STUN, alors que serveur et navigateur de test tournaient sur la même machine — le routeur de Jaslin refusait la boucle vers sa propre IP publique (NAT hairpinning non supporté), d'où l'échec systématique de connexion média ; corrigé en `use_external_ip: false` + `node_ip: 127.0.0.1` explicite (adapté pour du dev sur une seule machine, à changer pour une IP LAN/publique en usage réseau réel ou production). 2) Une fois la connexion établie, **aucun son ne passait** : `ParticipantTile.tsx` attachait la piste caméra à l'élément `<video>` mais n'attachait jamais la piste micro nulle part — et comme le `<video>` n'est même monté que si la caméra est active, un participant en audio seul (le scénario central de dégradation auto du brief) n'aurait de toute façon eu aucun élément média sur lequel jouer son micro. Corrigé par un `<audio>` dédié toujours monté pour chaque participant distant. **Les deux corrections confirmées fonctionnelles par Jaslin lui-même** (vidéo et son entre deux participants réels, hors sandbox). Comptes seed : demo@reyinyon.ht, mot de passe Reyinyon@123. Jamais commité en git au moment de l'écriture de cette entrée.

**Phase 3 — facturation et paiements (2026-07-09/10, sur "vas y" sans précision, suivant l'ordre du document lui-même)** : `Etablissement.tauxTaxe` (taxe locale configurable par établissement, jamais de taux global codé en dur), `Facture` générée automatiquement dans la même transaction que la `Reservation` (montantHT/taxes/montantTotal, statut IMPAYE/PARTIEL/PAYE dérivé), `Paiement` en historique multi-versements (remplace le champ `datePaiement` unique du sketch initial du document, insuffisant pour un vrai historique). Module `factures/` : consultation facture+historique, enregistrement de paiement avec garde explicite contre le surpaiement (rejet si montant > solde restant) et recalcul transactionnel du statut. Rapports étendus (établissement et chaîne) avec un bloc facturation par devise (facturé/payé/impayé), sur la date de facturation et non la date de séjour. Solde dû affiché à la réception à côté du bouton Check-out, sans bloquer le check-out (non demandé par le document pour cette phase). Vérifié en API (facture auto-cohérente avec le taux de taxe, paiement partiel → PARTIEL, surpaiement rejeté en 400, paiement complétant le solde → PAYE, double-paiement rejeté en 409, rapports établissement/chaîne corrects) et navigateur (ouverture de facture depuis `/reservations`, paiement réel via le formulaire, sections FACTURATION/FACTURATION CONSOLIDÉE visibles sur `/rapports` et `/chaine`). Après ces deux phases : `tsc --noEmit` propre partout, base remise à un état de seed propre, serveurs arrêtés. Reste (documenté, pas codé) : Phase 4 (channel manager), extension 5 étoiles, tests automatisés.

**Phase 2 livrée le 2026-07-09 (suite, même jour) : réception (check-in/check-out) + ménage.** `StatutChambre` étendu (`OCCUPEE`, `NETTOYAGE_EN_COURS`), nouveau modèle `TacheMenage` (générée automatiquement au check-out). Décision de conception clé documentée dans le plan : `OCCUPEE` ne doit jamais bloquer une réservation à dates futures (le chevauchement de `Reservation` suffit déjà), alors que `NETTOYAGE_EN_COURS` (comme `MAINTENANCE`) exclut la chambre de **toute** recherche tant que le ménage n'est pas marqué terminé, conformément au texte explicite du document source — `disponibilite.service.ts` et `reservations.service.ts::trouverChambreDisponible` corrigés en conséquence (`statut: { notIn: ['MAINTENANCE', 'NETTOYAGE_EN_COURS'] }`). Modules `reception/` (vue du jour, check-in/check-out avec compare-and-swap sur `Chambre.statut`) et `menage/` (liste des tâches, assignation à un employé MENAGE, progression À faire→En cours→Terminée avec CAS remettant la chambre `DISPONIBLE`) ajoutés côté backend ; pages `/reception` et `/menage` côté frontend, navigation Sidebar désormais strictement par rôle (RECEPTION/MENAGE/ADMINISTRATEUR_ETABLISSEMENT/ADMINISTRATEUR_CHAINE, chacun sa propre liste de liens). **Deux bugs réels supplémentaires trouvés et corrigés en vérification** : (1) incohérence de fuseau horaire latente depuis la Phase 1 — la garde "date d'arrivée pas dans le passé" comparait une date reçue en `YYYY-MM-DD` (parsée en minuit UTC par JS) à un minuit calculé en heure **locale** du serveur (America/Port-au-Prince, UTC-4), rejetant à tort toute réservation pour le jour même ; la vue du jour de la réception souffrait du même décalage sur ses bornes horaires ; les deux corrigés en calculant systématiquement le début de journée en UTC. (2) fuite de données : l'API ménage renvoyait le hash du mot de passe de l'employé assigné via un `include: { employeAssigne: true }` non filtré ; corrigé par un `select` explicite (`id`, `nom` uniquement). Vérifié en conditions réelles : API (check-in puis double check-in refusé 409, check-out crée bien une `TacheMenage` et bascule la chambre en nettoyage, double check-out refusé, recherche de disponibilité confirmée inchangée dans le futur pour une chambre `OCCUPEE` mais bien réduite pour une chambre `NETTOYAGE_EN_COURS` immédiate, tâche terminée remet la chambre disponible, double-terminaison refusée) et navigateur (Playwright, clics réels sur les boutons Check-in/Check-out et sur la progression des tâches de ménage, RBAC visuel confirmé pour les 4 rôles). `tsc --noEmit` propre. Base remise à un état de seed propre après vérification.

**Extension 5 étoiles, tranche 1 — Folio + Restaurant/Bar (2026-07-10, "CONTINUE" puis choix de portée clarifié avec Jaslin entre "Folio+Restaurant/Bar" et "tout construire d'un coup").** Décision d'intégration centrale, documentée dans le plan avant codage : le `Folio` de l'extension ("la facture maîtresse" du séjour) n'est PAS un système de paiement séparé — il est adossé à la `Facture` déjà construite en Phase 3. Chaque `LigneFolio` (charge restaurant/bar posée sur la chambre) fait grossir `Facture.montantTotal` (recalculé à neuf depuis la somme de toutes les `LigneFolio`, jamais un cumul incrémental) et déclenche `factures.service.ts::recalculerStatutPaiement()` (extraite en fonction partagée), réutilisant tel quel l'endpoint de paiement déjà vérifié en Phase 3 — aucun système de paiement parallèle écrit. **Changement de comportement assumé** : le document de l'extension exige qu'un folio ne se ferme que si son solde est réglé ; comme l'ouverture/fermeture du folio est calquée sur check-in/check-out, ceci annule la décision prise en Phase 3 ("pas de blocage de check-out sur impayé") — `reception.service.ts::checkout()` vérifie désormais le solde de la `Facture` avant toute écriture et rejette en 409 avec message clair si non réglé. Modèle ajouté : `Folio`/`LigneFolio`, POS `PointDeVente`/`Table`/`MenuItem`/`Commande`/`LigneCommande`, nouveau rôle `SERVEUR`. Backend `modules/folios/` (ouverture automatique au check-in, fermeture au check-out, ajout de ligne appelé uniquement depuis `restaurant.service.ts::cloturerCommande()` — jamais de route générique d'ajout de ligne) et `modules/restaurant/` (CRUD menu/tables admin établissement, prise de commande serveur, envoi cuisine, service, clôture avec choix "ajouter au folio chambre X" ou paiement direct pour un client de passage, jamais de facturation isolée). Frontend : `FactureModal` (déjà existant, Phase 3) étendu avec une section "Détail du folio" listant chaque `LigneFolio` — réutilisation de l'écran de clôture déjà construit plutôt qu'un nouvel écran, exactement comme demandé par le document ; nouvelles pages `/pos` (grille de tables, prise de commande, clôture), `/cuisine` (écran cuisine simple, liste des commandes envoyées), `/restaurant/menu` (CRUD carte). **Incident de vérification (pas un bug applicatif) :** rate limiter d'authentification (10 tentatives/15 min, en mémoire) atteint après de nombreux tests de connexion consécutifs pendant la vérification — résolu en redémarrant le processus backend (état en mémoire, pas en base) plutôt qu'en attendant la fenêtre. **Vérifié en conditions réelles** : API (curl — check-in ouvre bien le folio, commande restaurant clôturée sur le folio de la chambre 101 fait passer `Facture.montantTotal` de 7150 à 9050 HTG et le statut de PAYE-potentiel à IMPAYE, check-out refusé en 409 tant que le solde n'est pas réglé puis accepté et ferme le folio après paiement complet, commande d'un client non-résident payée en direct sans jamais toucher au folio, validation Zod rejetant une clôture sans `chambreNumero` ni `methodePaiement`) et navigateur (Playwright — connexion serveur, RBAC visuel confirmé, prise de commande réelle sur `/pos`, envoi en cuisine, `/cuisine` marque servie, clôture réelle au folio de la chambre 201, `FactureModal` affichant la section "Détail du folio" avec la ligne restaurant, solde dû visible à la réception). `tsc --noEmit` propre partout. Base remise à un état de seed propre après vérification (nouveaux comptes/données : `serveur@otela.ht`, 2 points de vente + tables + carte par établissement).

**Extension 5 étoiles, tranche 2 — Spa & Bien-être (2026-07-11, "CONTINUE", ordre du document suivi directement sans reclarifier la portée).** Même principe d'intégration que Restaurant/Bar : "ajout automatique au folio si le client est résident, paiement direct sinon" réutilise telles quelles `folios.service.ts::ajouterLigneFolio()` et `factures.service.ts::recalculerStatutPaiement()` — aucun nouveau système de paiement. Portée volontairement réduite sur un point que le document autorise explicitement lui-même ("réservation en ligne possible depuis le site public... **ou en interne**") : seule la prise de rendez-vous interne (réception/admin établissement) est construite, pas de nouvelle page publique de réservation spa. Pas de rôle dédié non plus (contrairement à `SERVEUR` pour le restaurant) — la réception gère les rendez-vous, un poste spa à part aurait été surdimensionné pour ce module. Modèle ajouté : `ServiceSpa` (nom, durée, prix), `Praticien`, `RendezVousSpa` (statut confirmé/terminé/annulé, `folioId` et `methodePaiement` nullable — même dualité que `Commande`). Chevauchement de créneaux vérifié en application (pas de contrainte d'exclusion Postgres comme pour les chambres, enjeu bien moindre) dans `spa.service.ts::creerRendezVous()` : aucun autre rendez-vous confirmé du même praticien ne doit chevaucher `[dateHeure, dateHeure + dureeMinutes)`. Backend `modules/spa/` (CRUD services/praticiens réservé à l'admin établissement, prise de RDV et clôture ouvertes à la réception), réutilise `clients.service.ts::findOrCreateClient()` déjà utilisée par les réservations de chambre. Frontend : pages `/spa` (liste du jour, prise de RDV, clôture avec le même choix folio/paiement direct que `/pos`) et `/spa/config` (CRUD services/praticiens) ; `FactureModal` déjà prêt pour le département SPA depuis la tranche 1, aucune modification nécessaire. **Vérifié en conditions réelles** : API (curl — création de RDV réussie, second RDV chevauchant sur le même praticien refusé en 409, clôture au folio de la chambre 101 faisant passer `Facture.montantTotal` de 7150 à 9650 HTG, validation Zod rejetant une clôture sans `chambreNumero` ni `methodePaiement`, paiement direct pour un client non-résident, annulation puis tentative de clôturer un RDV annulé refusée en 409) et navigateur (Playwright — connexion réception, prise de RDV réelle via le formulaire, clôture réelle au folio de la chambre 101, section "Détail du folio" de `FactureModal` affichant bien la ligne Spa). `tsc --noEmit` propre partout. Base remise à un état de seed propre après vérification (nouvelles données : 3 services + 2 praticiens par établissement).

**Extension 5 étoiles, tranche 3 — Minibar, Blanchisserie, Conciergerie, Voiturier (2026-07-11, "VAS Y" après confirmation du périmètre restant).** Les 4 modules que le document lui-même qualifie de "plus simples" traités en une seule tranche : chacun est un CRUD scopé directement à une `Chambre` (jamais un numéro saisi par un employé comme pour Restaurant/Spa), avec au plus une intégration folio optionnelle — pas de complexité de type POS. Nouvelle fonction `folios.service.ts::getFolioOuvertParChambreId()` (variante directe de la version par numéro déjà écrite). Trois logiques de facturation différentes selon le module, toutes documentées dans le plan avant codage : **Minibar** facture directement à chaque constat (`ConsommationMinibar` → `LigneFolio` immédiate, département `MINIBAR`) ; **Blanchisserie** facture automatiquement au passage du statut à `LIVREE` (service rendu, pas juste demandé — même logique que la clôture d'une commande restaurant) ; **Conciergerie** et **Voiturier** facturent de façon strictement optionnelle (`montant` fourni ou non à la clôture — "pas de logique financière complexe, sauf si..." pour la conciergerie ; "pas de facturation dans la plupart des hôtels, sauf si..." pour le voiturier), sans département dédié pour la conciergerie dans l'enum du document (utilise `AUTRE`). Aucun rôle dédié créé (comme pour Spa) : RBAC `MENAGE`+admin établissement pour le minibar (constat lors du contrôle de chambre), `RECEPTION`+admin établissement pour les 3 autres. Pas de sélecteur d'employé générique pour l'assignation en conciergerie (aurait nécessité un nouvel endpoint de liste globale des employés) — simplifié en un bouton "Prendre en charge" qui auto-assigne l'employé actuellement connecté. Backend : 4 nouveaux modules (`minibar/`, `blanchisserie/`, `conciergerie/`, `voiturier/`) suivant tous le même squelette service/controller/routes. Frontend : 4 pages correspondantes, toutes réutilisant les patterns déjà établis (liste + formulaire de création + actions inline, comme `/menage`) ; aucune modification de `FactureModal` nécessaire, tous les départements étaient déjà couverts depuis la tranche 1. **Vérifié en conditions réelles** : API (curl) — chaîne complète sur une même réservation, `Facture.montantTotal` suivi précisément à chaque étape (7150 base → 7650 après minibar → 8450 après blanchisserie livrée → 9950 après une demande de conciergerie payante de 1500 HTG, une demande gratuite n'ayant rien ajouté → 10250 après un supplément voiturier de 300), double-départ d'un véhicule refusé en 409. Navigateur (Playwright) : constat minibar réel via le formulaire (ménage), commande blanchisserie créée et menée jusqu'à Livrée via les vrais boutons de progression (réception), `/conciergerie` et `/voiturier` chargés sans erreur, `FactureModal` affichant bien les lignes Minibar et Blanchisserie. `tsc --noEmit` propre partout (un faux positif du cache `.next` généré, sans lien avec le code applicatif, résolu par un nettoyage du cache). Base remise à un état de seed propre après vérification (nouvelles données : 4 articles minibar par établissement).

**Extension 5 étoiles, tranche 4 — Room Service (2026-07-11, "continue").** Seul module de l'extension absent de la phrase de séquencement finale du document ("...puis Spa, puis les modules plus simples..., puis Événementiel et Fidélité en dernier") mais présent dans sa liste numérotée, juste après Restaurant/Bar — traité comme sa propre tranche plutôt que regroupé avec Minibar/Blanchisserie/Conciergerie/Voiturier, car sa vraie complexité (réutilisation du catalogue `MenuItem`, cycle de vie commande complet avec passage en cuisine) est plus proche de Restaurant/Bar que des modules "simples". **Décision clé documentée dans le plan** : le document dit que Room Service "peut réutiliser le menu du restaurant principal" — réutilisation du catalogue `MenuItem` uniquement, pas du modèle `Commande`/`Table`. Rendre `Commande.tableId` optionnel pour y greffer un `chambreId` alternatif aurait cassé la résolution d'établissement déjà écrite dans `restaurant.service.ts` (`table.pointDeVente.etablissementId`, utilisée partout) pour un gain de réutilisation marginal — trop de risque de régression sur un module déjà livré et vérifié. Nouveau modèle **parallèle** `CommandeRoomService`/`LigneCommandeRoomService`, scopé à `chambreId`, référençant le même `MenuItem`, réutilisant le même enum `StatutCommande` (pour apparaître naturellement aux côtés des commandes restaurant sur l'écran cuisine déjà construit). Différence de flux explicite dans le document : toujours facturé au folio, jamais de paiement direct (le client est forcément résident) — `cloturerCommande` n'a donc qu'un seul chemin, plus simple que son équivalent restaurant. Backend `modules/room-service/` (RBAC `SERVEUR`+admin établissement, même personnel que Restaurant/Bar). Frontend : page `/room-service` (sélection d'une chambre occupée, même sélecteur de menu que `/pos` mais un seul bouton de clôture "Facturer au folio") ; `/cuisine` étendue pour fusionner l'affichage des commandes restaurant et room service côte à côte, avec un badge "Room service" distinguant les deux ; aucune modification de `FactureModal` (département `ROOM_SERVICE` déjà couvert depuis la tranche 1). **Vérifié en conditions réelles** : API (curl — cycle complet ouverture→ajout→envoi cuisine→livraison→clôture, `Facture.montantTotal` passée de 7150 à 8100 HTG pour un plat à 950) et navigateur (Playwright — connexion serveur, commande réelle via le formulaire, apparition sur `/cuisine` avec le badge Room service, livraison marquée, une seconde commande facturée au folio dans la même session, ligne "Room service" visible dans `FactureModal` ; deux lectures prématurées du texte de la page dans le script de test ont donné un faux "non visible" avant que le fetch asynchrone ne se termine — vérifié directement en base que les deux commandes avaient bien atteint SERVIE et PAYEE, confirmant qu'il s'agissait d'un artefact du script de vérification et non d'un bug produit). `tsc --noEmit` propre partout. Base remise à un état de seed propre après vérification.

**Reste à faire** (documenté, pas codé, hors périmètre) : Phase 4 (channel manager Booking.com/Expedia, seul le champ `canal` est préparé), réservation spa en ligne côté site public, et 2 modules de l'extension 5 étoiles (Événementiel/Banquets, Programme de fidélité — volontairement en dernier selon l'ordre du document, logique différente : devis séparé pour l'un, programme transversal à toute la chaîne pour l'autre ; 7 des 9 modules livrés). Tests automatisés absents (cohérent avec le reste du portefeuille). Jamais commité en git au moment de l'écriture de cette entrée.

**Mise à niveau vers un cahier des charges SaaS Multi-Hôtel façon Cloudbeds (2026-07-22, 2 tranches, détail complet dans `context/HISTORY.md`).** Jaslin a fourni un cahier des charges de 22 points ; audit préalable montrant ~65 % du back-office cœur déjà couvert mais ~10 % du volet public/commercial. Trois décisions tranchées avant de coder (AskUserQuestion) : garder **Express** (pas de migration NestJS, le point « API documentée » livré via **Swagger** dérivé des schémas Zod sur `/api/docs`), **stockage disque local via multer** (pas Cloudinary), commencer par les fondations. **Tranche 1 (fondations)** : fiche établissement complète (logo, GPS, contacts, horaires, politique, devise principale, fuseau — ce dernier pour l'affichage uniquement, la logique de dates restant en UTC), 7 rôles (ajout de **Propriétaire** lecture seule chaîne, **Comptable**, **Maintenance** — sans renommer les enums, `ADMINISTRATEUR_CHAINE`/`_ETABLISSEMENT` restant Super Admin/Directeur au niveau interface), galerie photo par type de chambre (invariant une seule principale, `PhotoChambre`), statut `RESERVEE` (jamais dans l'exclusion de disponibilité — testé), ventilation adultes/enfants (invariant côté service), **journal d'audit** (`JournalAudit`, écriture explicite hors transaction dans les services sensibles, page `/journal` filtres + CSV), socle upload (`upload.middleware.ts`, multer, filtre MIME sans SVG). **Tranche 2 (parcours client final)** : `Reservation.reference` (`OT-XXXXXX` unique), email de confirmation enrichi avec **QR code** et lien de consultation, **facture PDF** (pdfkit, `facture.pdf.ts`, générée à la demande jamais stockée), **consultation en ligne sans compte** (`/ma-reservation`, référence + email vérifiant le titulaire), téléchargement PDF public et back-office (bouton dans `FactureModal`). **Vérifié en conditions réelles** : `tsc` propre, ~28 tests API cumulés et 16 vérifications navigateur (dont upload photo et téléchargement PDF réels), 2 bugs PDF trouvés en relisant le PDF généré (séparateur de milliers `fr-FR` rendu en `/` par Helvetica, débordement page) corrigés. Comptes seed ajoutés : maintenance@otela.ht, comptable@otela.ht, proprietaire@otela.ht (Otela@123). Dépendances : multer, swagger-ui-express, @asteasolutions/zod-to-openapi, pdfkit, qrcode. Deux migrations additives. **Reste du cahier des charges (non traité)** : signature numérique au check-in, notifications SMS/WhatsApp, exports Excel, dashboard graphique, site vitrine 10 pages + avis + SEO, paiements en ligne réels (bloqués sur credentials Digicel/MonCash). Commité en git à la fin de la session.

**Maintenance & Inventaire (2026-07-22, suite, points #13/#14 du cahier des charges) :** sur choix explicite de Jaslin parmi 4 options proposées (maintenance+inventaire retenu plutôt que dashboard/exports, site vitrine, ou signature+WhatsApp). Le rôle `MAINTENANCE` existait déjà (tranche Fondations) mais n'avait aucun écran à piloter — seule action possible : la bascule DISPONIBLE↔MAINTENANCE d'une chambre déjà en place. Nouveaux modèles `TicketMaintenance` (chambre **ou** zone commune en texte libre, jamais les deux — invariant Zod ; priorité BASSE/NORMALE/HAUTE/URGENTE ; statut A_FAIRE/EN_COURS/RESOLU ; `bloqueChambre` optionnel) et `ArticleInventaire`/`MouvementInventaire` (stock interne linge/consommables, distinct d'`ArticleMinibar` qui sert la facturation client). **Décision d'intégration centrale** : signaler un problème ne doit jamais échouer parce que la chambre est occupée — si `bloqueChambre` est demandé mais la chambre pas `DISPONIBLE`, le ticket est créé quand même, seul le flag est ramené à `false` (documenté, pas une erreur). Résolution d'un ticket bloquant : ne restaure `DISPONIBLE` que si aucun **autre** ticket encore ouvert avec `bloqueChambre:true` ne vise la même chambre (sinon un second problème en cours serait écrasé) — garde vérifiée en la testant réellement (deux tickets bloquants simultanés sur la même chambre, résolution du premier ne restaure pas, résolution du second restaure). Inventaire : mouvements ENTREE/SORTIE/AJUSTEMENT avec compare-and-swap sur les sorties (rejet 409 si stock insuffisant, jamais de stock négatif — même pattern que GESCOM/SHOPAY) ; AJUSTEMENT prend une valeur **absolue** (pas un delta) pour coller à un inventaire physique sans ambiguïté de signe ; historique `MouvementInventaire.stockApres` figé à chaque mouvement. RBAC : signalement ouvert à RECEPTION/MENAGE/MAINTENANCE/direction, gestion (statut/assignation) réservée à MAINTENANCE/direction ; usage quotidien de l'inventaire ouvert à MENAGE/MAINTENANCE/direction, création d'articles réservée à la direction. Pas de journal d'audit sur ces deux modules (cohérent avec menage/spa/room-service — l'audit reste réservé aux actions sensibles argent/sécurité/identité). **Vérifié en conditions réelles** : API (curl — les deux scénarios de garde anti-écrasement ci-dessus, cloisonnement établissement, RBAC 403 par rôle, double-résolution 409, ticket zone commune, validation Zod rejetant chambre+zone ou ni l'un ni l'autre, ENTREE/SORTIE/AJUSTEMENT avec `stockApres` cohérent, sortie en stock insuffisant 409, doublon de nom d'article 409) et navigateur (Playwright — connexion RECEPTION/MAINTENANCE/MENAGE/direction, création réelle d'un ticket zone commune via le formulaire, RBAC visuel confirmé (MENAGE sans contrôle d'assignation, sans bouton "Nouvel article"), création d'article + mouvement ENTREE réel avec badge "Stock bas" visible). **Incident d'environnement (pas un bug produit) :** `next/font/google` (Inter) nécessite le réseau pour télécharger la police, indisponible dans ce sandbox — `layout.tsx` temporairement modifié pour lever le blocage le temps du test navigateur, puis restauré à l'identique (diff vide confirmé) avant la fin de la session. `tsc --noEmit` propre backend et frontend. Base remise à un état de seed propre (`prisma migrate reset` + reseed), serveurs arrêtés par PID ciblé sur le port. **Reste du cahier des charges** : signature numérique au check-in, notifications SMS/WhatsApp, exports Excel, dashboard graphique, site vitrine + avis + SEO, paiements en ligne réels.

**Site vitrine + Avis + SEO (2026-07-22, suite, points #5/#19) :** Jaslin a choisi de bâtir les **pages à valeur réelle** plutôt que les 10 pages du cahier des charges au complet (pas de mentions légales/CGU/confidentialité/FAQ cette session — texte juridique générique hors périmètre produit). Nouveau modèle `Avis` (un par séjour, `reservationId @unique`, note 1-5, modération par masquage `visible:false` jamais suppression, réponse optionnelle de la direction). **Décision centrale** : soumission d'avis jamais un formulaire ouvert — réutilise la vérification référence+email de `reservations.service.ts::getReservationPublique` (même garde anti-énumération), rejette si le séjour n'est pas `TERMINEE` (400), unicité par réservation (409). Nouvel endpoint public `GET /etablissements/:id/vitrine` (chambres+photos+tarifs+avis visibles+moyenne en une seule requête composée). Nouveau module backend `avis/` (RBAC : `PROPRIETAIRE` lecture seule, modération réservée direction établissement/chaîne — cohérent avec le reste du portefeuille), nouveau `avisLimiter` (anti-spam, point d'écriture public). **Restructuration SEO obligatoire** : `'use client'` interdisant `export const metadata`, `/reserver` et `/ma-reservation` scindées en Server Component (`page.tsx`, metadata dynamique/statique) + Client Component (`ReserverClient.tsx`/`MaReservationClient.tsx`, logique inchangée). Nouvelle page `/etablissements/[id]` — **Server Component pur** (SSR réel) : `generateMetadata` dynamique, JSON-LD `schema.org/Hotel` (avec `aggregateRating` si avis existants), galerie chambres avec seul îlot interactif `Lightbox.tsx` (agrandissement au clic), CTA "Réserver ici" pré-remplissant `/reserver?etablissementId=`. Nouvelle page `/contact` (coordonnées réelles de tous les établissements, aucun formulaire backend — cohérent avec le reste du portefeuille). `app/sitemap.ts` + `app/robots.ts` natifs Next.js (App Router), `ma-reservation` exclue du sitemap et `noindex` (page de consultation privée). Back-office : nouvelle page `/avis` (modération masquer/répondre) + `avisStore.ts`. **Bug réel trouvé et corrigé en vérification** : le fetch de la page établissement utilisait `next: { revalidate: 60 }` (ISR) — un avis fraîchement soumis ou une réponse de la direction restait invisible jusqu'à 60 secondes, corrigé en `cache: 'no-store'` (la fraîcheur des avis prime sur la charge marginale, page peu visitée). **Vérifié en conditions réelles** : API (curl — cycle complet réservation→check-in→paiement→check-out→avis sur une vraie réservation de seed ; avis refusé tant que non `TERMINEE` ; double soumission 409 ; email/référence erronés → même message 404 ; `PROPRIETAIRE` 403 en écriture mais 200 en lecture ; masquage réel faisant disparaître l'avis de la vitrine publique ; réponse direction persistée) et navigateur (Playwright, 22/22 vérifications — JSON-LD présent, CTA pré-remplissant le formulaire de réservation, soumission réelle d'un avis via le formulaire `/ma-reservation`, apparition immédiate sur la fiche établissement après le correctif de cache, masquage back-office répercuté côté public, sitemap.xml/robots.txt valides). `tsc --noEmit` propre. Base remise à un état de seed propre, serveurs arrêtés par PID ciblé, Playwright installé `--no-save` puis désinstallé. **Reste du cahier des charges** : mentions légales/CGU/confidentialité/FAQ, paiements en ligne réels.

**Check-in signé + Notifications WhatsApp (2026-07-23, points #8/#17) :** sur choix explicite de Jaslin parmi les deux chantiers restants (signature+WhatsApp retenu plutôt que les pages légales). `Reservation.signatureUrl`/`signatureDate` ajoutés (migration additive), le check-in exige désormais une signature — refusé en 400 sans fichier, cohérent avec l'intention du document (le check-in EST le moment de la signature, pas une étape optionnelle). Nouveau sous-dossier `uploads/signatures` (même pattern multer que logos/photos, filtre image sans SVG). Frontend : `SignaturePad.tsx` (canvas, Pointer Events unifiant souris/tactile pour une tablette de comptoir, mise à l'échelle des coordonnées car la résolution interne du canvas diffère de sa taille CSS étirée) + `CheckinModal.tsx` (bouton de confirmation désactivé tant qu'aucun trait n'est dessiné). Sur `/reception`, le bouton Check-in ouvre ce modal ; une arrivée déjà signée affiche un badge "Signé ✓" à sa place.

**WhatsApp (même philosophie qu'ASSOCOTISE/REYINYON, jamais d'envoi automatisé)** : nouveau `lib/whatsapp.ts::construireLienWhatsApp` (normalisation du numéro haïtien, préfixe 509 si absent). Bouton "Confirmer par WhatsApp" sur l'écran de confirmation public (`ReserverClient.tsx`) et sur `/ma-reservation` (client-déclenché, non journalisé). Bouton "Rappel WhatsApp" sur les arrivées du jour de `/reception` (personnel-déclenché) : ouvre le lien puis journalise via un nouvel endpoint `POST /reservations/:id/whatsapp-log` (nouvelle valeur `WHATSAPP_ENVOYE` dans `ActionAudit`) — atteste que la démarche a été déclenchée, pas que le message a été reçu, même nuance déjà écrite explicitement sur ASSOCOTISE. **Vérifié en conditions réelles** : API (curl — check-in refusé 400 sans signature, réussi avec un vrai PNG dont le fichier servi est identique octet pour octet à l'original, double check-in toujours refusé 409 par la garde chambre déjà existante, format non-image rejeté 400, `whatsapp-log` consultable dans `/journal`) ; navigateur (Playwright — signature réellement tracée à la souris sur le canvas puis check-in complet de bout en bout, badge "Signé" affiché ensuite, parcours de réservation publique complet jusqu'à confirmation avec le bouton WhatsApp). **Incident de vérification (pas un bug produit)** : le popup `wa.me` n'était pas détectable par Playwright faute de réseau vers WhatsApp dans ce sandbox (même limitation que `next/font/google` rencontrée plus tôt dans le portefeuille) — confirmé en instrumentant `window.open` côté page pour capturer l'URL construite sans naviguer réellement : lien et message corrects dans les deux cas (rappel personnel et confirmation client). `tsc --noEmit` propre, base remise à un seed propre, fichiers de signature de test supprimés, serveurs arrêtés par PID ciblé, Playwright installé `--no-save` puis désinstallé. **Reste du cahier des charges** : mentions légales/CGU/confidentialité/FAQ, paiements en ligne réels (#10, bloqués sur credentials Digicel/MonCash).

**Pages légales + FAQ (2026-07-23, dernier chantier actionnable du cahier des charges) :** sur « CONTINUE » alors qu'il ne restait plus qu'un seul chantier réalisable (les paiements en ligne restant bloqués sur les credentials Digicel/MonCash externes). Quatre nouvelles pages Server Component statiques (mêmes conventions SEO que `/contact` — `metadata` par page) : `/mentions-legales`, `/cgu`, `/confidentialite`, `/faq`. **Contenu juridique volontairement générique**, rédigé pour Haitech Solutions/OTELA mais explicitement commenté dans chaque fichier comme un modèle à faire relire par un vrai juriste avant tout usage réel — décision déjà actée lors de la clarification initiale sur l'ampleur de la vitrine. La FAQ reste honnête sur l'état réel du produit plutôt que de promettre une fonctionnalité non livrée : la question sur les moyens de paiement précise que le règlement se fait **actuellement sur place**, le paiement en ligne n'étant pas encore actif (cohérent avec le point #10 toujours bloqué). FAQ construite en accordéon (`FaqAccordion.tsx`, seul îlot client, même principe que `Lightbox.tsx` sur la fiche établissement — le contenu textuel reste server-rendered pour le SEO). Les 4 pages ajoutées au footer public (`(public)/layout.tsx`) et à `app/sitemap.ts`. **Vérifié en conditions réelles** : `tsc --noEmit` propre ; navigateur (Playwright) 15/15 vérifications — titre et H1 corrects sur les 4 pages, liens du footer présents, accordéon FAQ fonctionnel (ouverture par défaut de la première question, bascule au clic), contenu du paiement confirmé honnête. Playwright installé `--no-save` puis désinstallé, serveur arrêté par PID ciblé (aucun changement backend ce chantier, pas de reset de base nécessaire). **Cahier des charges SaaS Multi-Hôtel de 22 points désormais entièrement traité ou explicitement documenté comme bloqué** : seul le point #10 (paiements en ligne réels) reste en attente des credentials Digicel/MonCash de Jaslin. Tests automatisés toujours absents sur l'ensemble du portefeuille.

**Dashboard graphique + Exports Excel (2026-07-22, suite, points #6/#15) :** sur choix explicite de Jaslin parmi 4 options proposées (dashboard+exports retenu plutôt que signature+WhatsApp ou pages légales). Skill `dataviz` chargée avant tout code de graphique. **Décision de conception** : pas de nouvelle bibliothèque de charts — cohérent avec le reste du portefeuille (toute l'UI déjà construite à la main) — deux composants SVG maison (`LineChart`, `BarChart`) réutilisant les tokens de couleur déjà existants d'OTELA (`--color-primary-3` bleu, `--color-accent` or) plutôt que la palette de substitution de la skill, la marque ayant déjà ses propres couleurs établies partout dans le produit. Pas de mode sombre (confirmé qu'aucune page du back-office OTELA n'en a). ADR/RevPAR traités comme des `StatCard` (chiffres uniques), pas des graphiques — règle de la skill "un chiffre isolé est une tuile". `rapports.service.ts` étendu : `adrParDevise`/`revparParDevise`/`repartitionParType` (par type de chambre) sur l'agrégat existant, nouvelle `getSerieJournaliere` (un point par jour, revenu réparti au prorata des nuits plutôt qu'attribué en bloc à l'arrivée, pour corréler avec l'occupation). Nouveau module `rapports.export.ts` (`exceljs`, dépendance réelle ajoutée) : classeurs "Résumé" + "Réservations" détaillées, pour établissement et pour la chaîne. **Bug réel trouvé et corrigé en vérifiant que la somme de la série journalière égalait l'agrégat existant** : la borne de fin de boucle utilisait l'horodatage brut de `dateDepart` au lieu de son jour calendaire tronqué, comptant systématiquement une nuit fantôme le jour du check-out — corrigé en bornant sur `debutJour(dateDepart)`. **Second bug mineur trouvé** : nom de fichier accentué ("Pétion-Ville") invalide dans l'en-tête `Content-Disposition` (HTTP n'accepte que de l'ASCII sans encodage RFC 5987) — corrigé par une translittération ASCII avant utilisation dans le nom de fichier. **Vérifié en conditions réelles** : API (curl, données de test insérées directement via Prisma — l'API rejette à raison toute date d'arrivée passée pour un vrai client — puis remises à zéro par `prisma migrate reset`), série journalière recoupée avec l'agrégat au nuit et au HTG près, les deux classeurs Excel relus programmatiquement (pas seulement code 200) confirmant des données exactes. Navigateur (Playwright) : 12/12 vérifications, dont l'infobulle au survol, la bascule "Voir les données", et un vrai téléchargement de fichier Excel non vide déclenché par le bouton. Capture d'écran de la page `/rapports` revue visuellement (étape finale de la skill) : rendu propre, sans chevauchement, cohérent avec le design system. `tsc --noEmit` propre, base remise à un seed propre, serveurs arrêtés par PID ciblé, Playwright installé `--no-save` puis désinstallé (seul `exceljs` reste, en dépendance permanente). **Reste du cahier des charges** : mentions légales/CGU/confidentialité/FAQ, paiements en ligne réels.

**Tarif Day-Use / séjour à la journée (2026-07-12, "VAS Y" sur plan écrit) :** second des deux chantiers validés par Jaslin après le module Gestion des Employés (le premier), suite à sa question sur les clients ne réservant pas une nuit complète. Audit préalable : `Reservation`/`Tarif` n'avaient aucune notion de durée hors nuitée (`differenceEnNuits` arrondissait un séjour de quelques heures à 0 nuit → prix nul). Bonne nouvelle confirmée en exploration : la contrainte anti-double-booking PostgreSQL (`EXCLUDE USING gist` sur `chambreId` + `tsrange`) et le check-in/check-out protègent déjà nativement un séjour de quelques heures sans aucune modification — seul le calcul du prix et l'UI de saisie manquaient. Nouvel enum `TypeSejour` (`NUITEE`/`JOUR`), champ ajouté à `Tarif` (JOUR = montant forfaitaire, jamais multiplié) et à `Reservation` (persisté pour affichage/rapports), migration purement additive. Règle métier JOUR : arrivée et départ doivent tomber le même jour calendaire en UTC (même convention que le reste du codebase), sinon 400 — nouvelle fonction `reservations.utils.ts::estMemeJourCalendaireUTC`. `disponibilite.service.ts` et `reservations.service.ts` filtrent désormais les tarifs sur `typeSejour` en plus de devise/dates. Correctif de cohérence dans `rapports.service.ts::getOccupationEtRevenu` : un séjour JOUR aurait été arrondi à 0 nuit dans `nuitsOccupees` (sous-comptant silencieusement l'occupation) — compté séparément dans un nouveau `sejoursJourCount`, le revenu restant correct pour les deux types puisqu'il ne dépend que de `montantTotal`. Frontend : nouveau `TarifModal.tsx` (trou identifié en exploration — il n'existait auparavant aucun moyen d'ajouter un tarif à un type de chambre existant après sa création initiale, seul `TypeChambreModal` le permettait à la création), bouton "+ Tarif" sur `/chambres` avec badge "forfait jour", toggle Nuitée/Day-use sur `ReservationModal.tsx` (back-office, date+heures au lieu de deux dates), badge type de séjour + affichage horaires sur la liste `/reservations`, même toggle sur le site public `/reserver` (recherche, cartes résultat "Forfait journée", écran de confirmation avec horaires). **Vérifié en conditions réelles** : API (curl — tarif JOUR créé, réservation même jour à prix forfaitaire exact 2500 HTG non multiplié, réservation à cheval sur deux jours rejetée en 400, double-booking croisé NUITEE↔JOUR testé dans les deux sens sur la même chambre avec succès en 409/201, `/disponibilite?typeSejour=JOUR` filtrant correctement les types sans tarif JOUR, rapport confirmant `sejoursJourCount: 3` et `nuitsOccupees: 2` séparés correctement, réservation day-use du jour confirmée visible dans arrivées ET départs de `/reception/vue-du-jour`) et navigateur (Playwright — site public bout en bout jusqu'à confirmation avec horaires, back-office ajout de tarif JOUR via le nouveau bouton, création manuelle day-use, badge visible dans la liste, réception). `tsc --noEmit` propre backend et frontend. Base de données remise à un état de seed propre après vérification (`prisma migrate reset` + reseed), serveurs arrêtés. **Roadmap des deux chantiers produit validés par Jaslin (gestion des employés + tarif day-use) entièrement clôturée.**
- **ACADÉMIE (UJEPH)** : système de gestion académique universitaire, développé par Jaslin pour son université (UJEPH) et **déjà en production** sur `ujeph-haiti.edu.ht`. **Découvert non documenté en session le 2026-07-23** (dossier `livrables/applications/academie/`, dépôt git séparé déjà poussé sur GitHub — `Jassage/academie-reprise-generator`, ~20 commits). Stack : Express 5 + TypeScript + Prisma + MySQL (backend) + Vite + React 18 + shadcn/ui (frontend, scaffoldé initialement via Lovable, dépendance `lovable-tagger` présente). ~40 modules : étudiants, professeurs, notes/bulletins/relevés, présence, inscriptions, UE/prérequis, rattrapages, emplois du temps, paiements/frais/bourses, bibliothèque (emprunts), messagerie interne, événements, annonces, salles/réservations, certificats, analytics, audit, sauvegardes. Rôles : Admin/Professeur/Secretaire/Directeur/Doyen.

  **Audit sécurité effectué le 2026-07-23** sur demande explicite de Jaslin ("agis comme un développeur senior, analyse-le") — **analyse seule, aucun correctif appliqué** ("documente les, on les fera après"). Projet fonctionnellement mature mais avec des failles critiques activement exploitables en production : (1) **deux endpoints d'inscription publics sans authentification** (`POST /api/auth/register` et `POST /api/users/register`, dans `auth.Controllers.ts` et `userController.ts`) acceptant le champ `role` en clair — n'importe qui peut se créer un compte Admin, la faille la plus grave trouvée sur tout le portefeuille à ce jour ; (2) **`JWT_SECRET="change_me"`** dans `.env`, valeur littéralement utilisée par le serveur (pas juste un fallback) — permettrait de forger un token pour n'importe quel rôle si la même valeur est déployée en prod (à vérifier par Jaslin) ; (3) **23 fichiers de routes sur 39 sans aucun middleware d'authentification** (présence, audit, bibliothèque, certificats, documents, messagerie, bourses, frais étudiants, salles, emplois du temps, annonces, analytics, années académiques, rattrapages, prérequis...) — API ouverte en lecture ET écriture sur des données financières et des messages privés ; (4) **route fantôme non protégée** `GET /api/audit/backup/sql` (`auditRoutes.ts`) réutilisant la fonction d'export SQL complet de la base (y compris hash des mots de passe) alors que la route officielle équivalente `/api/backup/export` est bien protégée par `authenticateToken` ; (5) mot de passe root MySQL et mot de passe d'application Gmail personnel de Jaslin (`jslnoccius@gmail.com`, utilisé comme SMTP de l'université) en clair dans `.env` local — non poussé sur git, `.gitignore` du backend correct, seul `gestion-frontend/.env` (juste `VITE_API_URL`) est traqué. RBAC quasi inexistant : `requireRole` défini dans `auth.middleware.ts` mais utilisé dans seulement 2 fichiers de routes sur 39 (`uERoutes`, `eventRoutes`) — partout ailleurs, dès qu'il y a une auth, c'est juste "connecté ou pas", sans distinction de rôle. Bug fonctionnel trouvé au passage : `verifyPassword` (`auth.Controllers.ts:402-490`) inachevée, s'arrête après le décodage du JWT sur un commentaire `// ... reste du code inchangé ...` sans jamais renvoyer de réponse (requête bloquée jusqu'au timeout côté client). Points positifs confirmés : verrouillage de compte après 5 tentatives (30 min), reset password par token opaque à expiration réelle (1h), journal d'audit réellement branché sur beaucoup d'actions sensibles, structure controllers/routes/services propre et lisible.

  **Correctifs appliqués le 2026-07-23 (suite, même jour, sur "vas y" puis "continue")** : les 3 points urgents du plan traités. (1) `POST /api/auth/register` et `POST /api/users/register` protégés par `authenticateToken + requireRole(["Admin"])` — création de compte réservée à un Admin déjà connecté. (2) `JWT_SECRET` régénéré (96 caractères hex aléatoires) dans le `.env` local — **à reporter manuellement sur le `.env` du serveur de production**, sinon aucun gain réel ; rotation qui déconnecte tous les utilisateurs actifs au prochain redémarrage. (3) Route fantôme `GET /api/audit/backup/sql` supprimée. (4) `authenticateToken` ajouté aux 24 fichiers de routes qui n'avaient aucune protection (le compte final était 24, pas 23). (5) Deux restrictions de rôle ciblées, les seules pour lesquelles la règle métier était sans ambiguïté : `/api/audit` réservé à Admin/Directeur, et les actions structurelles d'années académiques (`initialize-base`/`sync`/`create-future`/`set-current`, opérations de maintenance ponctuelles et non des gestes quotidiens d'un rôle métier) réservées à Admin. `studentFeeRoutes` aligné sur la convention de ses modules frères (`authenticateToken, deanPermissions`). (6) `verifyPassword` (bug trouvé en audit) complétée : récupère l'utilisateur depuis le token, compare le mot de passe avec bcrypt, journalise et renvoie enfin une réponse — elle ne bloquait plus indéfiniment la requête.

  **Choix assumé de ne pas aller plus loin sur le RBAC fin** (créer/modifier/supprimer par rôle, module par module, sur les ~20 modules restants — bibliothèque, salles, messagerie, bourses, certificats, emplois du temps, annonces...) : deviner qui a le droit de faire quoi sans connaître l'organisation réelle de l'UJEPH (qui gère la bibliothèque ? qui gère les salles ?) risquait de casser des usages légitimes sur un système déjà en production, ce qui aurait été pire que le laisser à "tout utilisateur connecté peut agir" — déjà une amélioration considérable par rapport à l'accès public. En attente des règles réelles de Jaslin pour aller plus loin. `tsc --noEmit` propre après chaque étape.

  **Vérification anti-régression (même session, avant de considérer le point 2 terminé)** : bonne surprise, l'intercepteur Axios central du frontend (`services/api.ts`) attache déjà `Authorization: Bearer` sur chaque requête dès qu'un token existe en `localStorage`, et aucune route publique de type inscription/signup n'existe côté frontend (seules `/login`, `/reset-password`, `/forgot-password` sont publiques dans `App.tsx`) — donc `authenticateToken` ajouté partout ne casse aucun usage réel actuel. **Découverte au passage, non corrigée** : `UsersManager.tsx`/`usePermissions.ts` contiennent une logique Doyen à moitié construite ("un doyen ne peut créer que Professeur/Secretaire") mais l'onglet "Utilisateurs" n'est listé que dans les modules accessibles à Admin (`getAccessibleModules()`) — un Doyen ne peut donc pas réellement atteindre cet écran aujourd'hui, ma restriction `requireRole(["Admin"])` sur `/auth/register` ne casse donc rien de réel, mais si Jaslin veut un jour activer le self-service Doyen, il faudra élargir cette règle. **Deux vrais bugs pré-existants trouvés et corrigés en vérifiant l'impact de mes changements** (composants utilisant `fetch()` brut au lieu du client axios central, donc sans le token automatique) : `documentStore.ts` (génération/aperçu/téléchargement de documents) et `AuditLogsManager.tsx` (journal d'audit) n'envoyaient aucun `Authorization` — corrigés en ajoutant l'en-tête manuellement (`localStorage.getItem("authToken")`, la bonne clé, différente de la clé `"token"` utilisée par erreur dans `PaymentManager.tsx` — bug pré-existant distinct, non corrigé, hors périmètre). Au passage, l'URL d'export du journal d'audit dans `AuditLogsManager.tsx` pointait vers un chemin inexistant (`/api/audit-logs/export` au lieu de `/api/audit/audit-logs/export`) — corrigée. Ces deux composants utilisent par ailleurs une URL d'API codée en dur (`http://localhost:4000`), un bug distinct antérieur à cette session qui les rendait déjà non fonctionnels en production indépendamment de l'authentification — signalé à Jaslin, non corrigé (hors périmètre du correctif sécurité). `tsc --noEmit` propre backend et frontend après ces correctifs. **Commité et poussé sur "vas y" (2026-07-23)** : commit `06fa6ba` sur `main`, 29 fichiers modifiés (les 26 backend + 2 frontend + le controller `auth.Controllers.ts`). GitHub signale au passage que le dépôt a été renommé/déplacé vers `https://github.com/Jassage/ujeph.git` (le push est passé par la redirection automatique) — à mettre à jour côté remote local si Jaslin veut. **Toujours pas déployé en prod** : le `JWT_SECRET` régénéré n'existe que dans le `.env` local, il doit être reporté manuellement sur le serveur de production pour que la faille soit réellement corrigée en prod (le commit ne contient aucun secret, `.env` reste ignoré par git).

  **RBAC fin, sur "la suite" (2026-07-23, suite)** : Jaslin a répondu à 4 questions ciblées (AskUserQuestion) sur les modules restants les plus sensibles. Règles obtenues et appliquées (`requireRole` sur tout le module, `router.use`) : bourses (`scholarshipRoutes`/`scholarshipApplicationRoutes`/`scholarshipDocumentRoutes`) → Admin/Secretaire/Directeur ; certificats (`certificateRoutes`) → Admin uniquement ; bibliothèque (`bookRoutes`/`bookLoanRoutes`) → Admin/Secretaire ; salles (`roomRoutes`/`roomEquipmentRoutes`) → Admin/Secretaire, mais **réservations de salle laissées ouvertes à tout connecté** (`roomReservationRoutes` inchangé) sur choix explicite de Jaslin ("tous réservent"). **Vérification anti-régression avant d'appliquer** : recherche exhaustive dans le frontend (`grep` sur scholarship/certificate/book/room dans tout `src/`) — confirmé qu'aucun écran réel n'appelle ces 6 endpoints aujourd'hui, les seules références trouvées (`academicStore.ts`) sont un store Zustand 100% local (mutateurs en mémoire, aucun `fetch`/`axios`), reliquat d'un prototype antérieur au vrai backend. Donc restriction sans aucun risque de casser un usage réel actuel — mais confirme aussi qu'une bonne partie des "~40 modules" du backend sont du scaffolding CRUD généré (probablement par `prisma-zod-generator`) jamais branché à une vraie page. `tsc --noEmit` propre.

  **Dernière passe sur "faire toutes les corrections" (2026-07-23, suite)** : audit élargi à tous les modules encore ouverts à "connecté sans distinction de rôle" — recherche exhaustive (`grep`) de leur usage réel côté frontend. Découverte majeure : `attendanceRoutes`/`scheduleRoutes` sont, comme les bourses/certificats/livres/salles, câblés côté écran (`AttendanceManager.tsx`) sur `useAcademicStore`, un store Zustand **100 % local sans aucun appel réseau réel** — et `retakeRoutes` n'est pas utilisé non plus, la fonctionnalité "reprise" réelle passant en fait par `/api/grades` (`gradeStore.ts`, appel `api.post("/grades", ...)`, avec un appel à `/grades/:id/retakes` explicitement commenté dans le code). Conclusion : une bonne partie du portefeuille "~40 modules" (bourses, certificats, livres, salles, présence, emplois du temps, reprises, annonces, messagerie, pièces jointes, participants événement, prérequis, niveaux de faculté) n'a **aucun consommateur frontend réel** — probablement du CRUD généré automatiquement (`prisma-zod-generator`) jamais branché à un écran. Pour `attendanceRoutes`/`scheduleRoutes`, restriction quand même appliquée par cohérence avec le modèle de permissions déjà écrit (mais jamais appliqué côté serveur) dans `usePermissions.ts` du frontend : présence → Admin/Doyen/Professeur, emplois du temps → Admin/Doyen. **Pour les modules confirmés sans aucun usage réel et sans le moindre signal de règle métier** (annonces + pièces jointes, participants événement, messagerie + pièces jointes, prérequis, niveaux de faculté, reprises) : laissés à "authentifié suffit", faute de toute base pour deviner une règle plus fine sans risquer d'être arbitraire. `tsc --noEmit` propre. **Bilan final RBAC** : 39/39 fichiers de routes protégés par authentification, 12 avec une restriction de rôle explicite (les modules sensibles ou pour lesquels une règle claire existait), le reste laissé ouvert à tout compte authentifié. **Commité et poussé** : commit `41401f0` sur `main` (10 fichiers). L'ensemble de l'audit sécurité de la session (découverte → correctifs → RBAC fin) tient en deux commits, `06fa6ba` puis `41401f0`. **Reste hors de ma portée** : reporter le `JWT_SECRET` régénéré sur le `.env` du serveur de production, et déployer (tirer les commits + redémarrer) — nécessite un accès serveur que je n'ai pas.

  **Refonte des frais de scolarité + revue module par module (2026-07-26)** : Jaslin a demandé de reprendre le modèle des frais sur celui du projet [[sygs-imfp-tenant-architecture|SYGS-IMFP]] — attribuer un frais à toute une faculté (au lieu d'un étudiant à la fois) puis pouvoir appliquer une réduction individuelle — et une revue élargie ("verifier tous les fichiers"). Référence IMFP inspectée (`gestion-backend/src/services/studentFeeService.ts`) : `assignFeeToClassLevel` (attribution en masse à un niveau, doublons ignorés) et `applyDiscount` (recalcul de `totalAmount` à partir d'`originalAmount`, jamais cumulatif). Adapté à académie : `FeeStructure.facultyId`/`level` (académie a un vrai modèle `Faculty`, contrairement à IMFP qui n'a que `ClassLevel`), `StudentFee.originalAmount`/`discountAmount`/`discountReason`. Nouveaux endpoints `POST /student-fees/assign-faculty` (`assignFeeToFaculty`, basé sur `Enrollment.facultyId`+`level`+`status=Active`) et `PATCH /student-fees/:id/discount` (`applyDiscount`). UI ajoutée dans `FeeStructureManager.tsx` (badge faculté/niveau, dialog d'attribution en masse) et `StudentFeesSection.tsx` (dialog de réduction avec calcul du nouveau montant en direct).

  **Bug réel trouvé en creusant l'usage réel de `assignFeeToStudent`** : le flux `StudentFeeAssignment.tsx` (composant jamais rendu nulle part — mort) postait vers `/student-fees/assign` avec un champ `academicYear` (libellé d'affichage, ex. "2025-2026") au lieu de `academicYearId` — 404 puis 400 en cascade si jamais branché. Corrigé dans `feeStructureStore.ts` (signature + nom de champ) et alias de route `/student-fees/assign` ajouté côté backend (le flux réellement utilisé, `EnrollmentManager.tsx` via `useEnrollmentStore.assignFeeToStudent`, était lui déjà correct et n'a pas été touché).

  **Revue rapide des ~40 controllers pour bugs évidents** (grep patterns : `new PrismaClient()` dupliqués, incohérences évidentes) — 8 fichiers corrigés en petites diffs : `auth.Controllers.ts`, `courseAssignmentControllers.ts`, `eventController.ts`, `facultyController.ts`, `professeurController.ts` créaient chacun leur propre `new PrismaClient()` au lieu du singleton partagé `../prisma` (risque d'épuisement du pool de connexions sous charge — pattern déjà signalé comme bonne pratique ailleurs dans le portefeuille) ; `enrollmentController.ts::normalizeStatus` comparait des clés `"Active"/"Completed"/"Suspended"` après un `.toUpperCase()` sur la valeur entrante — la comparaison ne matchait jamais, donc **tout import d'inscriptions retombait silencieusement sur le statut par défaut "Active" quel que soit le statut réel dans le fichier importé** ; `studentController.ts` : `req.user.id` sans optional chaining pouvait crasher si `user` undefined ; `gradeRoutes.ts` : route montée en double (`/api/grades/grades/check-assignments` au lieu de `/api/grades/check-assignments`, inatteignable) ; `feePaymentController.ts::updateFeePayment` : bloc de gestion d'erreur Zod laissé en commentaire (`// ... gestion erreur ...`) sans jamais renvoyer de réponse 400 — complété.

  **Accès base de données obtenu (2026-07-26)** : Jaslin a mis à jour `DATABASE_URL` dans le `.env` local avec un mot de passe MySQL valide. Découverte en lançant `prisma migrate status` : 9 migrations antérieures au 30 septembre 2025 apparaissaient comme "non appliquées" alors que leurs tables existaient déjà — la table `_prisma_migrations` ne contient des enregistrements qu'à partir du 30 septembre (la BDD réelle a clairement été construite via `prisma db push` avant cette date, puis l'équipe est passée aux migrations proprement dites). Vérifié rigoureusement avant de toucher à quoi que ce soit : `prisma migrate diff --from-url ... --to-schema-datamodel` a confirmé que le **seul** écart entre la base réelle et le schéma cible était mes ajouts sur les frais — donc sans risque, les 9 migrations ont été baselinées (`migrate resolve --applied`) puis ma nouvelle migration déployée (`migrate deploy`) sans toucher au reste.

  **Vérification end-to-end réelle (2026-07-26)** : `tsc --noEmit` propre backend/frontend, backend démarré, testé via curl module par module avec un compte Admin temporaire (créé directement en base après blocage du classificateur auto-mode sur l'écriture directe, autorisation explicite demandée et obtenue de Jaslin). Confirmé : login émet un token valide avec le nouveau `JWT_SECRET` ; `/auth/register` et `/audit/audit-logs` renvoient bien 401 sans token ; création d'une structure de frais avec `facultyId`/`level` persistés correctement ; `assign-faculty` attribue aux inscriptions actives correspondantes et ignore les doublons au second appel ; `applyDiscount` recalcule `totalAmount` correctement, rejette une réduction supérieure au montant initial (400), et remet `totalAmount` à `originalAmount` quand `discountAmount` repasse à 0 ; modules à rôle restreint (bourses, certificats) toujours accessibles à Admin après migration ; 404 catch-all intact. Données de test et compte Admin temporaire supprimés après vérification, serveur arrêté proprement.

  **Rien commité pour cette passe** — en attente de validation de Jaslin avant commit/push.

  **Frais de scolarité par faculté + réductions, et revue de code élargie (2026-07-23, suite, "faire toutes les corrections")** : Jaslin a demandé de reprendre le modèle de frais façon SYGS-IMFP (l'autre projet de gestion scolaire du portefeuille) — attribuer un frais à toute une faculté d'un coup plutôt qu'étudiant par étudiant, puis pouvoir appliquer des réductions individuelles — et de vérifier plus largement le reste du code. Exploration d'IMFP (`livrables/applications/IMFP_PROTOTYPE`) comme référence directe : `StudentFee.originalAmount/discountAmount/discountReason` (le total dû est toujours recalculé depuis le montant d'origine, jamais cumulé) et un `assignFeeToClassLevel` bulk sur les inscriptions actives. **Découverte en creusant l'historique des migrations d'academie** : `fee_structures` avait à l'origine (migration `20250930192119_init_models_capitalized`) des colonnes `faculty`/`level` NOT NULL avec une contrainte unique `(academicYear, faculty, level)` — supprimées plus tard **sans migration Prisma correspondante** (dérive schéma/historique, symptôme de plus d'un `db push` ou édition manuelle), et le composant frontend `StudentFeeAssignment.tsx` (jamais rendu nulle part, code mort) référence encore ces champs disparus (`fee.faculty`, `fee.tuitionFee`, `fee.otherFees.library`...) — confirmant que la scolarité par faculté existait bel et bien avant d'être retirée, exactement ce que Jaslin demandait de restaurer.

  **Livré** : `FeeStructure.facultyId`/`level` (nullables, relation vers `Faculty`) et `StudentFee.originalAmount`/`discountAmount`/`discountReason` ajoutés au schéma. Nouveau `assignFeeToFaculty` (bulk, calqué sur `assignFeeToClassLevel` d'IMFP : cible les inscriptions `Active` d'une faculté + niveau optionnel pour une année académique, ignore les doublons) et `applyDiscount` (PATCH, recalcule `totalAmount = originalAmount - discountAmount`, ajuste le statut) côté backend, routes `POST /student-fees/assign-faculty` et `PATCH /student-fees/:id/discount`. Frontend : formulaire de création de structure de frais étendu (sélecteur Faculté + champ Niveau dans `FeeStructureManager.tsx`), nouveau dialog "Attribuer à la faculté", et nouveau dialog "Appliquer une réduction" dans `StudentFeesSection.tsx` (Admin uniquement, même garde que le reste des actions financières de cet écran) avec affichage du montant d'origine et de la réduction en cours. **Bug corrigé au passage** : `POST /student-fees/assign` était appelé par le frontend (`feeStructureStore.ts`) sans jamais exister côté backend (seul `POST /student-fees` existait) et avec le mauvais nom de champ (`academicYear` au lieu de `academicYearId`) — alias de route ajouté et champ corrigé ; seul consommateur de ce chemin (`StudentFeeAssignment.tsx`) confirmé être du code mort, donc aucune régression possible, mais correction faite par cohérence.

  **Blocage migration** : impossible de se connecter à MySQL depuis cette session (`root`/mot de passe du `.env` refusés par le service `MySQL94`, pourtant démarré) — migration écrite à la main (`prisma/migrations/20260723225320_fee_faculty_and_discount/migration.sql`, purement additive : nouvelles colonnes nullables ou à valeur par défaut, aucune donnée existante touchée) mais **jamais appliquée ni vérifiée en conditions réelles**. Le client Prisma a été régénéré (`prisma generate`, ne nécessite pas de connexion) donc le code compile contre le nouveau schéma, mais tourner ce code contre la vraie base plantera tant que la migration n'est pas appliquée. **Jaslin doit fournir les vrais identifiants MySQL locaux ou appliquer lui-même la migration.**

  **Revue de code élargie (agent Explore dédié + vérification manuelle des points les plus importants)** sur les controllers restants (grade, payment, feePayment, enrollment, attendance, schedule, courseAssignment, student, professeur) : 5 bugs réels **corrigés** — `normalizeStatus` dans `enrollmentController.ts` comparait des clés `Active`/`Completed`/`Suspended` à un lookup `.toUpperCase()`, ne matchant jamais et retombant systématiquement sur `"Active"` peu importe le statut réel importé (impact réel sur l'import Excel/CSV des inscriptions) ; un `catch` de validation Zod totalement vide dans `feePaymentController.ts::updateFeePayment` laissait passer des données non validées ; 5 controllers (`auth.Controllers.ts`, `courseAssignmentControllers.ts`, `eventController.ts`, `facultyController.ts`, `professeurController.ts`) instanciaient chacun leur propre `PrismaClient` au lieu du singleton partagé — risque réel d'épuisement du pool de connexions en production — unifiés sur `../prisma` ; route `gradeRoutes.ts` avec un chemin dupliqué (`/grades/grades/check-assignments`) corrigée ; accès `req.user.id` sans optional chaining dans `studentController.ts` (seul endroit du fichier à ne pas le faire) sécurisé. **Trouvé mais non corrigé** (documenté pour Jaslin, décisions produit hors de mon périmètre) : `updateEnrollmentStatus`/`updatePaymentStatus` côté frontend appellent des routes `/status` qui n'existent pas côté backend — mais confirmé que ni l'une ni l'autre fonction n'est jamais appelée depuis un écran réel (code mort, même famille que `StudentFeeAssignment.tsx`) ; `paymentController.ts` n'a aucune validation Zod ni audit log contrairement à son module frère `feePaymentController.ts` qui traite les mêmes données financières ; `attendanceController.ts`/`scheduleController.ts` confirmés sans consommateur frontend réel (cf. session RBAC précédente) ; `Payment.academicYearId` a un `@default("2024-2025")` en dur sur un champ qui est en réalité une clé étrangère. `tsc --noEmit` propre backend et frontend après chaque étape. **Rien commité ni poussé pour ce chantier** — en attente que Jaslin donne accès à MySQL pour vérifier réellement avant de commit.
- **NEXORA** : plateforme SaaS multi-tenant combinant gestion d'entreprise, analytics et un « Business Copilot » IA pour PME (dépôt git séparé, `livrables/nexora-platform/`). Découvert non documenté en session le 2026-07-26 : projet 100 % documentaire, aucune ligne de code produit, aucun client, aucune traction — seulement de la stratégie et de l'architecture papier. **Audit critique complet réalisé** (rôle CTO/CPO/conseil stratégie demandé explicitement par Jaslin) : la documentation existait en **trois taxonomies concurrentes non réconciliées** empilées au fil de générations successives (dossiers thématiques `00-07`, dossiers par domaine technique, couche « bibles »/constitution), avec pour conséquence 3 PRD dupliqués, 3 Go-To-Market, 4 roadmaps, 3 schémas de base de données, un index officiel (`BIBLE-001`) déjà faux par rapport au disque, et une contradiction technique réelle (`system-architecture.md` hésitait encore entre MySQL et PostgreSQL alors qu'un ADR tranchait déjà PostgreSQL). Trou le plus critique trouvé : **aucun modèle d'isolation multi-tenant n'était spécifié nulle part** malgré "multi-tenant" répété comme argument commercial, et **aucun fournisseur de LLM n'était nommé** pour le Copilot, le différenciateur produit central.

  **Mon avis critique donné sans complaisance avant d'agir (demandé explicitement par Jaslin)** : la thèse (copilote de décision pour PME, démarrage Haïti) est solide et bien argumentée, mais le projet cumule des signaux d'alerte réels — périmètre trop large pour un MVP (ERP + analytics + IA + automatisation à la fois, face à Odoo/QuickBooks/agents IA natifs simultanément), aucune preuve de validation client documentée (personas et hypothèses de marché jamais confrontés à un vrai entretien), le ratio documentation/produit totalement inversé pour ce stade (85 documents pour zéro ligne de code et zéro client), et un risque de dispersion vu l'ampleur déjà considérable du reste du portefeuille de Jaslin (15+ projets actifs). Recommandation donnée : valider un vertical unique auprès de vrais dirigeants avant d'écrire davantage de documents.

  **Restructuration complète exécutée sur validation de Jaslin** ("faites tout"), en deux passes le même jour. Passe 1 : nouvelle arborescence unique `NEXORA-OS/` (`00-FOUNDATION` → `12-LEGAL` + `_meta/` gouvernance), suppression de tous les doublons confirmés, correction de la contradiction PostgreSQL, et surtout rédaction des décisions manquantes critiques — **ADR-005 (isolation multi-tenant par Row-Level Security PostgreSQL + discriminateur `tenant_id`, doc d'implémentation `multi-tenancy-architecture.md`)** et **ADR-006 (abstraction fournisseur LLM `LLMProvider`, RAG scopé tenant via pgvector plutôt que fine-tuning, garanties contractuelles DPA/non-entraînement exigées)**, plus les ADR-003/004 (React, Prisma) qui étaient listés "Accepted" sans fichier. Passe 2, sur demande explicite de lisser et compléter chaque fichier : réécriture en prose fluide de ~48 documents hérités (style fragmenté façon transcription de slides), rédaction intégrale des 30 squelettes restants (P0 détaillé, P2 avec substance réelle), avec une règle stricte assumée sur les documents sensibles — `12-LEGAL/` rédigé comme plans de rédaction avec avertissement de relecture juriste obligatoire (jamais de faux texte légal prêt à publier), `financial-model.md`/`investor-overview.md` donnent la méthode sans jamais inventer de chiffres de traction/CAC/LTV inexistants. Corrections de cohérence trouvées en vérifiant plutôt qu'en faisant confiance à l'audit initial : nomenclature de plans tarifaires unifiée (3 schémas incompatibles à prix fixes non validés coexistaient), modèles de données manquants du schéma canonique réintégrés (`Category`/`Expense`/`AIConversation`/`AuditLog`), section RAG/Embeddings absente du document d'architecture Copilot canonique réintégrée, ~15 liens relatifs cassés entre dossiers corrigés après vérification programmatique. Deux commits de fond (`75bd378` restructuration, `e4f7b87` réécriture de contenu), précédés d'un commit de sauvegarde de l'état d'origine. **Statut réel inchangé par cette session : toujours pré-MVP, zéro ligne de code produit, zéro client, zéro traction** — seule la qualité et la cohérence de la documentation ont changé.

  **Développement démarré le 2026-07-27 (session 1 — Fondation backend, sur "on attaque le développement de nexora").** Avant tout code, lecture du `mvp-implementation-plan.md`, des ADR structurants et du `data-model.md`, puis une seule décision remontée à Jaslin (AskUserQuestion, pour/contre honnête) : le framework backend. L'ADR-002 fixe NestJS mais tout le portefeuille de Jaslin est en Express ; **Jaslin a explicitement choisi de suivre l'ADR (NestJS)** pour la structure long terme, en assumant la courbe d'apprentissage. Stack retenue : **NestJS + Prisma v5 + PostgreSQL (RLS) + Next.js**. Code dans `livrables/nexora-platform/backend/` (NestJS) et `frontend/` (Next.js scaffoldé, App Router + Tailwind + TS, pas encore de code applicatif). Ports : backend **4009**, frontend **3010**, Postgres Docker **5433** (conventions du portefeuille). Prisma auto-installé en v7 (breaking) puis **fixé en v5** pour rester cohérent.

  **Isolation multi-tenant (cœur du produit, ADR-005) — implémentée et prouvée.** `docker-compose.yml` avec Postgres 16 **+ pgvector** (activé dès le départ pour le RAG P1, pas de migration ultérieure). Deux rôles Postgres créés par script d'init : `nexora_app` (LOGIN, **non-superuser, non-BYPASSRLS**, propriétaire des tables mais soumis au RLS grâce à `FORCE ROW LEVEL SECURITY`) pour tout le runtime, et `nexora_auth` (**BYPASSRLS**) réservé à la SEULE opération cross-tenant nécessaire : la recherche d'utilisateur par email au login. Schéma Prisma de 11 modèles MVP (Organization/User/RefreshToken/Customer/Category/Product/Sale/SaleItem/Expense/AIConversation/AuditLog), tout mappé snake_case. Migration RLS écrite à la main : `ENABLE`+`FORCE ROW LEVEL SECURITY` et une policy `tenant_isolation` sur chaque table, fail-closed via `nullif(current_setting('app.current_org', true), '')::uuid` (le `nullif` corrige un piège réel trouvé en test : un GUC personnalisé renvoie une chaîne vide, pas NULL, une fois `set_config` appelé sur la connexion, ce qui faisait planter `''::uuid`). Le tenant courant est posé par transaction (`PrismaService.forTenant()` pour les requêtes unitaires, `runInTenant()` pour les écritures multi-tables atomiques), pattern d'extension Prisma officiel, et vient **toujours** du token d'auth vérifié, jamais d'un paramètre client. **Test d'isolation qui mord (5/5 vert, vraie base, rôle applicatif)** : tenant B aveugle aux données de A, fail-closed sans contexte, `WITH CHECK` rejetant une écriture avec un `organization_id` étranger.

  **Auth complète (vérifiée en HTTP réel).** register (crée l'organisation + son premier utilisateur ADMIN, id d'org généré côté app pour que le `WITH CHECK` passe sans BYPASSRLS), login (message générique + comparaison bcrypt systématique anti-énumération/anti-timing), refresh **par rotation** (le refresh token est un JWT signé contenant `sub`+`org` — ce qui résout le login/refresh sous RLS puisque l'org vient du JWT vérifié — et son hash SHA-256 est stocké en base pour la révocation réelle ; l'ancien est révoqué à chaque rotation), logout (révocation), `/me`. Cookie httpOnly (`SameSite=Lax`, `path=/api/auth`), politique mot de passe 12 caractères, RBAC (`@Roles`/`RolesGuard`), `ValidationPipe` en `whitelist`+`forbidNonWhitelisted` (anti mass assignment), helmet, CORS crédential. Module Organization (consulter/mettre à jour l'entreprise, PATCH réservé ADMIN). Vérifié : 201 register, 401/200/401 login (mauvais mdp / correct / email inconnu), 409 email dupliqué, 400 mot de passe faible, 401 sans token, rotation (ancien cookie → 401), logout puis refresh → 401, PATCH devise en ADMIN, isolation `/organizations/me` A≠B. **Bug réel trouvé et corrigé** : `prisma migrate reset` recrée le schéma `public` et efface les droits posés par le script d'init du conteneur → `nexora_auth` perdait `USAGE`/`SELECT` et le login tombait en 500 ; corrigé en déplaçant les GRANT minimaux (`USAGE ON SCHEMA public`, `SELECT ON users` uniquement) DANS la migration RLS, qui est rejouée à chaque reset. `npm run build` (tsc) propre. Guide de démarrage écrit (`DEV.md`). **Rien commité** (attente validation Jaslin).

  **Frontend — fondation livrée dans la même session (2026-07-27), sur demande explicite de déclencher les skills design.** Skills `frontend-design` + `ui-ux-pro-max` invoquées ; le défaut « bleu corporate SaaS + ambre » du générateur volontairement écarté (que `frontend-design` proscrit) au profit d'une direction ancrée dans le sujet (copilote pour entrepreneurs haïtiens) : **ancre teal profond + or chaud comme unique signature** sur neutre chaud, display **Space Grotesk**, corps **Inter**, chiffres tabulaires **IBM Plex Mono**, marque « aiguille de boussole » teal à pointe or. Stack : **Next.js 16 (App Router, Turbopack) + React 19 + Tailwind v4** (tokens en `@theme`), port **3011** (3010 déjà pris par ASSOCOTISE, corrigé après coup — CORS backend mis à jour). Système de design dans `globals.css`, primitives (Button/Field/Input/StatCard), icônes SVG inline (jamais d'emoji). Auth côté client : `lib/api.ts` (access token en mémoire, `credentials: include` pour le cookie, auto-refresh sur 401 avec verrou anti-concurrence), `AuthProvider` (hydratation via `/auth/refresh` au montage). Écrans : login + register (deux-panneaux, panneau marque `brand-aurora` + jauge de force du mot de passe), shell applicatif (sidebar + topbar responsive avec drawer mobile, modules à venir marqués « Bientôt »), dashboard (KPI branchés sur `/organizations/me`, carte copilote, premiers pas), page Paramètres (édition nom/devise réservée ADMIN via PATCH). **Vérifié en conditions réelles** : `tsc --noEmit` propre, `next build` propre (8 routes), et flux complet via l'UI dans le navigateur (inscription → cookie de session → redirection dashboard → chargement du vrai nom d'organisation scopé RLS ; register renvoie 201, CORS préflight 204). Limite d'environnement : les clics/touches synthétiques n'atteignent pas le pane navigateur quand il est masqué (d'où vérification par soumission programmatique + réseau + arbre d'accessibilité plutôt que par clic réel) — pas un défaut applicatif. Compte de démo créé en base : demo-web@nexora.ht / MotDePasse1!. **Reste des P0** : Clients, Produits, Ventes (backend + UI) ; puis dashboard branché sur de vraies ventes, puis Business Copilot V1 (P1).

  **Session 2 (2026-07-27, "on continue avec nexora") — les 4 derniers P0 livrés, tout le plan MVP hors Copilot est fait.** Backend : module **Clients** (liste/recherche, historique de ventes, création/modification réservées ADMIN+MANAGER), **Produits + Catégories** (catalogue avec prix/quantité/disponibilité, création de catégorie à la volée), **Ventes** (le module le plus sensible : décrément de stock **atomique par compare-and-swap** dans une transaction `runInTenant`, `updateMany({quantity: {gte: demande}})` qui verrouille la ligne le temps de la transaction — testé en conditions réelles avec une vente de 100 unités contre un stock de 7, rejetée en 409 propre, stock inchangé après rollback), **Dashboard** (chiffre d'affaires du mois + tendance vs mois précédent, nombre de ventes, top 5 produits, série 7 jours). RBAC aligné sur la matrice de permissions documentée dans `authn-authz-policy.md` : USER peut vendre et consulter mais pas créer/modifier clients ou produits.

  Frontend : trois nouvelles pages (Clients, Produits avec sélection/création de catégorie inline, Ventes avec formulaire multi-lignes et total calculé en direct) au même design system que la fondation (nouveaux composants Modal/Badge/Select/Textarea), dashboard entièrement re-câblé sur les vraies données (sparkline 7 jours en CSS pur, checklist « Premiers pas » devenue dynamique et masquée une fois tout accompli), navigation débarrassée des badges « Bientôt » sur ces trois modules (seul Copilote reste à venir).

  **Incident d'environnement (pas applicatif)** : le cache Turbopack de `next dev` s'est corrompu après un `next build` lancé en parallèle sur le même dossier (`Persisting failed: Unable to write SST file`) — résolu en tuant tous les process Node et en supprimant `.next`. Un `.next` de build stale datant de la veille avait aussi provoqué de faux 404 sur `/login` au premier démarrage du jour, même cause.

  **Vérifié en conditions réelles de bout en bout** : `tsc --noEmit` et `next build` propres des deux côtés, RBAC/validation Zod/anti mass-assignment testés en curl (401 sans token, 400 champ non déclaré, 400 catégorie vide), puis un parcours complet **en navigateur avec de vraies créations** (connexion démo, ajout d'un client "Boutique Coin de Rue" via le formulaire, création d'une vente de 2 Coca-Cola pour ce client, stock passé de 7 à 5 dans le catalogue, chiffre d'affaires du dashboard passé à 375 HTG). **Commité** (`daad8c5`, premier commit de code du projet — la fondation de la session précédente n'avait jamais été commitée non plus, tout est parti dans ce commit). **Reste : Business Copilot V1 (P1)**, seul chantier du plan MVP non commencé.

  **Session 3 (2026-07-27, "vas y") — Business Copilot V1 (P1), tout le plan MVP est livré.** Sur l'ADR-006 (abstraction `LLMProvider` obligatoire, anti-lock-in ; RAG via pgvector réservé aux documents libres, pas aux données structurées) : interface `LLMProvider` (`generate`) injectée par token Nest (`LLM_PROVIDER`), implémentation `AnthropicProvider` (`@anthropic-ai/sdk`, modèle configurable via `ANTHROPIC_MODEL`, défaut `claude-sonnet-5`). **Décision prise sans bloquer sur la clé API** : sans `ANTHROPIC_API_KEY` configurée, le service dégrade proprement vers le contexte d'entreprise brut au lieu d'échouer ou d'inventer une réponse — jamais de fausse génération IA silencieuse, principe direct de l'« IA explicable » du document produit. Context Builder (nouveau `CopilotService`) : agrège les vraies données via `DashboardService` (désormais exporté par `DashboardModule`) + une requête stock faible dédiée (seuil 5 unités, pas de champ de seuil par produit dans le schéma — choix pragmatique documenté) pour produire un texte structuré (CA du mois, tendance, top produits, alertes stock), utilisé à la fois comme grounding du prompt et comme réponse dégradée. Conversations persistées dans `AIConversation` (déjà modélisé depuis la fondation, aucune migration nécessaire). Garde-fou de coût minimal (ADR-006 décision 4) : quota de 30 questions/jour/organisation (compteur sur `AIConversation`, pas de système de plans tarifaires — absent du MVP). RBAC : aucune restriction de rôle, cohérent avec « voir le dashboard » ouvert à tous.

  Frontend : page `/copilote` (chat avec historique, suggestions rapides reprenant les 3 fonctions V1 documentées dans `copilot-architecture.md` — résumé, analyse ventes, alertes stock), carte « Votre copilote » du dashboard mise à jour avec un lien réel, badge « Bientôt » retiré de la sidebar (plus aucun module de la nav n'est en attente).

  **Vérifié en conditions réelles** : `tsc --noEmit` propre, `POST /copilot/ask` et `GET /copilot/history` testés en curl en mode dégradé (aucune clé configurée dans cet environnement), quota quotidien confirmé (30 questions acceptées, la 31e rejetée en 429), parcours complet en navigateur (question posée depuis l'UI, réponse basée sur les vraies données affichée avec le badge « IA non configurée », lien fonctionnel depuis le dashboard). Données de test du quota nettoyées en base après vérification. **Commité** (`a003d67`). Reste à activer une vraie clé Anthropic (décision de Jaslin, coût réel) pour des réponses génératives plutôt que le mode dégradé, et à valider l'usage avec de vrais dirigeants avant d'aller plus loin (recommandation de l'audit stratégique du 2026-07-26, toujours valable : zéro client à ce jour).

  **Session 4 (2026-08-09) — Gestion des utilisateurs (P1), dernier chantier du plan MVP : le plan est désormais entièrement clos.** Audit préalable révélant que ce point du plan (invitation, rôles, permissions au sein d'une même entreprise) restait le seul non traité malgré la mention précédente : `register` ne créait que le premier ADMIN, aucun moyen d'inviter un second utilisateur dans une organisation existante n'existait. Sur choix de Jaslin (AskUserQuestion) : invitation par **lien à usage unique envoyé par email** (plutôt qu'un mot de passe initial fixé par l'ADMIN façon OTELA). Nouveau modèle `Invitation` (token opaque haché SHA-256, jamais stocké en clair, même principe que `RefreshToken`) **avec RLS** comme le reste des tables métier. Résoudre une invitation par token avant que l'invité n'ait de JWT est le même problème de bootstrap que le login par email : réutilisation telle quelle d'`AuthPrismaService` (BYPASSRLS) pour cette seule lecture cross-tenant, jamais pour la création du compte qui repasse par le client scopé au tenant (`runInTenant`), exactement comme `register`. Nouveau `MailService` (nodemailer, repli en journal si `SMTP_HOST` absent, même philosophie que POSTA/ASSOCOTISE — l'invitation réussit toujours, même sans SMTP réel). Anti-lockout, cœur de la logique métier : un ADMIN ne peut ni changer son propre rôle ni se désactiver lui-même, et aucune action ne peut faire tomber le nombre d'ADMIN actifs d'une organisation à zéro — cette dernière garde vérifiée y compris contre le cas réel où elle a une utilité (un token JWT encore valide 15 min d'un compte tout juste désactivé par un tiers ne peut pas non plus désactiver le dernier ADMIN restant, la vérification se fait sur l'état réel en base, pas sur les claims du token). Désactiver un compte révoque désormais aussi ses refresh tokens actifs (même réflexe que le reste du portefeuille). Frontend : page `/parametres/utilisateurs` (liste des membres + invitations en attente, changement de rôle, activation/désactivation, révocation d'invitation) accessible depuis une nouvelle carte sur `/parametres` réservée ADMIN, et `/accepter-invitation/[token]` (page publique, crée la session comme après un register classique). **Incident d'environnement rencontré et résolu** : après une interruption inattendue ayant coupé les deux serveurs de dev, le frontend redémarré servait un faux 404 sur la nouvelle route — cache `.next` périmé datant du 27/07 (même cause déjà documentée sur ce projet le 2026-07-27), résolu en le supprimant avant de relancer. **Vérifié en conditions réelles** : cycle complet côté API (curl — invitation, repli SMTP avec lien correct dans les logs, preview publique, acceptation avec émission de session, réutilisation du token refusée en 404, RBAC 403 sur un non-ADMIN, auto-modification refusée en 403, promotion de rôle, garde du dernier ADMIN vérifiée avec un token stale en 409, révocation d'invitation puis double-révocation refusée en 404, révocation du refresh token à la désactivation confirmée par un cycle refresh 200→401) et navigateur (Playwright, scénario de bout en bout : inscription → invitation → acceptation → changement de rôle → désactivation, captures d'écran confirmant un rendu cohérent avec le design system). `tsc --noEmit` propre backend et frontend. Base remise à un état propre après chaque vérification, serveurs arrêtés (zombies Windows sur les ports 4009/3011 tués explicitement, `TaskStop` seul ne suffisant pas). Commité (`0463119`). **Tous les P0 et P1 du plan MVP NEXORA sont désormais réellement et intégralement livrés.**

  **Session 5 (2026-08-13) — Revue senior dev et durcissement sécurité.** Analyse du code réel (pas de la doc) à la demande de Jaslin. **Verdict : le socle est solide**, isolation RLS complète et vérifiée table par table (12 tables métier, 12 policies, toutes en `ENABLE` + `FORCE`, aucun oubli), surface BYPASSRLS minuscule et justifiée (2 méthodes, les deux étant de vrais problèmes de bootstrap), décrément de stock correct en compare-and-swap. Un soupçon a été **infirmé par la mesure** plutôt qu'affirmé : le `DUMMY_HASH` anti-timing du login paraissait mal formé (61 caractères au lieu de 60), test empirique fait, il fonctionne réellement (741 ms contre 634 ms de référence). **Trois vrais trous trouvés et corrigés, plus un correctif de config** (branche `durcissement/rate-limit-audit-rls`, commit `bc6216e`, jamais fusionnée dans master ni poussée, dépôt local sans remote) : (1) **aucun rate limiting nulle part** alors que tout le reste du portefeuille en a (POSTA, KONEKTE, BANKA) — `@nestjs/throttler` v6 ajouté, politiques centralisées dans `common/throttle.ts` (garde-fou global 300/min, login 10/15min, register 5/h, refresh 60/15min volontairement plus large car plusieurs utilisateurs partagent souvent une IP derrière un NAT en Haïti, invitation 10/h), plus un `trust proxy` conditionnel (`TRUST_PROXY`) sans lequel, derrière nginx/Railway, toutes les requêtes sembleraient venir du proxy et la limite deviendrait globale pour tous les clients d'un coup ; (2) **la table `audit_logs` existait avec son RLS et sa policy mais aucune ligne n'y était jamais écrite** (zéro occurrence dans `src/`), branchée sur 9 actions à liste fermée, avec deux modes délibérément asymétriques — `recordInTx()` écrit DANS la transaction de l'action auditée (intégrité prioritaire : impossible de modifier un rôle sans trace) et `record()` non bloquant pour le login (disponibilité prioritaire : un incident de journal ne doit pas empêcher une connexion légitime) ; (3) **le RLS reposait entièrement sur la discipline humaine**, rien ne garantissant qu'une table ajoutée plus tard soit protégée — nouveau `test/rls-coverage.e2e-spec.ts` sans aucune liste en dur, qui interroge le catalogue PostgreSQL pour découvrir les tables portant un `organization_id` et exige de chacune ENABLE + FORCE + au moins une policy ; (4) `.env.example` annonçait le port frontend 3010 au lieu de 3011 et le fallback CORS de `main.ts` avait la même valeur périmée (un clone frais suivant la doc démarrait avec un CORS cassé). **Deux affirmations prouvées expérimentalement plutôt que déclarées** : l'atomicité de l'audit (contrainte `CHECK` posée pour saboter l'écriture d'audit, changement de rôle réel tenté, HTTP 500 et rôle **resté inchangé**, donc rollback confirmé des deux écritures, contrainte retirée et requête rejouée en 200) et le fait que le test de garde RLS **mord vraiment** (table `table_piege` non protégée créée, test échouant immédiatement en la nommant, piège retiré, retour à 10 tests verts). **Vérifié en conditions réelles** : `tsc --noEmit` propre, 10 tests e2e verts, serveur réellement démarré et interrogé (leçon REMED : `tsc` seul ne suffit pas) — 429 obtenu à la 11e tentative de login pendant que `/auth/me`, `/dashboard/summary` et `/users` répondaient toujours 200 (throttle bien cloisonné par route, pas d'auto-déni de service), trace d'audit complète obtenue sur un cycle invitation → acceptation → changement de rôle → désactivation → réactivation, et confirmation que les requêtes bloquées en 429 n'écrivent rien dans le journal (le bruteforce ne peut pas le noyer). Base remise à son état initial (1 utilisateur, 1 organisation, 0 audit), serveur arrêté par PID, aucun zombie. **Incident à signaler : la clé API Anthropic réelle présente dans `backend/.env` s'est affichée en clair dans le transcript de session** (censure ratée dans une commande de vérification) — le fichier est correctement gitignoré et la clé n'a jamais été commitée (vérifié par `git log -S` sur tout l'historique), mais **la rotation de cette clé a été recommandée à Jaslin**. Points restants non traités, documentés : 3 vulnérabilités `npm audit` de niveau high préexistantes, et un changement de rôle qui met jusqu'à 15 min à prendre effet (le rôle est lu depuis le JWT, compromis assumé du JWT sans état, la désactivation restant elle immédiate côté sessions). **L'avis stratégique reste inchangé et devient plus pressant : le code n'est plus le goulot d'étranglement, la Phase 1 de la roadmap NEXORA (`company-roadmap.md`) est une phase de validation terrain sans une seule ligne de code à écrire (15 à 20 entretiens, choix d'un vertical unique, pilote gratuit), et le projet a toujours zéro client.**

  **Session 5 bis (2026-08-13/14) — « Est-ce prêt à encaisser des abonnements ? » : non, et pas « presque ».** Vérifié par recherche exhaustive backend et frontend : **aucune ligne de code capable d'encaisser de l'argent** (le seul résultat pour `subscription`/`stripe`/`moncash`/`billing`/`payment`/`plan`/`trial`/`checkout` est un commentaire disant que la facturation est absente du MVP), et aucun modèle `Plan`/`Subscription`/`Payment`/`Invoice` parmi les 12 de la base. `register` crée aujourd'hui une organisation avec un accès complet, illimité et gratuit à vie. **Deux autres trous rédhibitoires pour un service payant** : aucun parcours de récupération de mot de passe (routes d'auth limitées à register/login/refresh/logout/me, un client enfermé dehors n'a aucun recours) et aucun artefact de déploiement (pas de Dockerfile applicatif, pas de CI, le `docker-compose.yml` ne servant qu'au Postgres de dev). Également signalés : `12-LEGAL/` ne contient que des plans de rédaction (pas de CGU ni de politique de confidentialité réelles, or on ne prélève pas d'argent sans), délivrabilité SMTP non prouvée, et compteurs de rate limiting en mémoire donc remis à zéro à chaque redémarrage et non partagés entre instances (stockage Redis nécessaire en multi-instance). **Avis : ne pas construire la facturation maintenant**, faute de savoir quoi facturer et à quel prix ; encaisser un éventuel premier client **à la main** (MonCash direct, reçu manuel, tout étant de toute façon illimité) et ne bâtir le système qu'avec trois clients réels et un prix validé. **Deux trous dans mon propre travail de la session 5, trouvés en répondant à « est-ce complet à 100 % ? » et depuis comblés** : `AUTH_LOGOUT` et `INVITATION_REVOKED` avaient été écrits mais **jamais exécutés une seule fois**, désormais testés et fonctionnels (y compris la garde anti-doublon du logout rejoué). **Finition livrée (commit `7c04d78`)** : le message de limitation renvoyait « ThrottlerException: Too Many Requests » affiché tel quel par le frontend (exception technique anglaise dans une interface française), remplacé par un message indiquant quand réessayer ; et les 20 messages d'erreur du backend, écrits sans accents alors que l'interface est correctement accentuée, corrigés (commentaires du code laissés sans accents, convention du projet). **Première passe navigateur réelle** (Playwright) : connexion via l'UI, **7 pages sur 7 en HTTP 200 sans une seule erreur console**, message de limitation capturé à l'écran dans sa bannière après la 11e tentative, rendu jugé soigné. Branche `durcissement/rate-limit-audit-rls` à 2 commits, toujours non fusionnée et non poussée.
- **Guide Ultime du Déploiement** (`livrables/guides/deploiement-ultime/`) : manuel de formation professionnelle sur le déploiement d'applications sur serveur Linux, écrit pour un débutant complet, style éditorial façon O'Reilly/Packt/Manning. Achevé le 2026-07-27 : 28 chapitres + Annexes (14 tableaux) + Glossaire compilé + Index, ~120 600 mots sur 35 fichiers Markdown. Chaque chapitre suit un gabarit strict en 23 sections (objectifs, concepts, schémas Mermaid, laboratoires, quiz, glossaire, FAQ...), rédigé chapitre par chapitre avec validation explicite avant de passer au suivant, plan éditorial validé en amont (`PLAN-EDITORIAL.md`). Les dix derniers chapitres sont des études de cas complètes (React+Express+PostgreSQL, React+NestJS, Next.js+Prisma+Docker, Laravel+Docker, Django, Spring Boot, ASP.NET, WordPress, ERPNext, Odoo) du serveur vierge à la prod sécurisée. Distinct de l'aide-mémoire opérationnel `livrables/guides/deploiement-serveur.md` (checklist rapide, réservée au quotidien une fois ce manuel maîtrisé). Potentiel de réutilisation direct comme matériel de cours vu mon activité d'enseignement de la programmation. Export HTML/DOCX/PDF pas encore fait, à la demande seulement.
- **REMED** : SaaS de gestion de pharmacie (générique, pas de client identifié pour l'instant). Stack habituelle du portefeuille : Express 4 + TypeScript + Prisma + PostgreSQL (backend, port 4011) + Next.js App Router (frontend, port 3013). Démarré le 2026-08-10, périmètre validé avec moi (AskUserQuestion) : Vente/POS, Stock, Achats/Fournisseurs, Ordonnances, Rapports — phasage complet dans `livrables/applications/REMED/PLAN.md`. **Phase 0 (socle) livrée et vérifiée le jour même** : auth JWT (access 15 min + refresh 30j rotatif en cookie httpOnly haché SHA-256) + RBAC 5 rôles (SUPER_ADMIN, GERANT, PHARMACIEN, VENDEUR, MAGASINIER), schéma Prisma complet du domaine (produits, lots/péremption, mouvements de stock, fournisseurs, commandes d'achat, ventes, ordonnances, caisse, audit — les modèles Achats/Ventes/Ordonnances/Caisse sont posés mais sans API dessus pour l'instant), modules Produits/Catégories, Stock (entrée par lot avec numéro de lot + date de péremption, ajustement protégé par compare-and-swap, journal de mouvements append-only, alertes stock bas + péremption < 90 jours), Fournisseurs, et un Dashboard (valeur du stock, alertes, fournisseurs actifs). La quantité en stock n'est jamais stockée sur le produit : toujours recalculée comme la somme des lots, pour ne jamais diverger du détail de traçabilité par lot. Frontend avec design system teal clinique dédié (dérivé du design system GESCOM/ANTENN). Vérifié en conditions réelles : `tsc --noEmit` propre des deux côtés, API testée au curl (login/refresh/rotation/logout, RBAC 403 sur VENDEUR, entrée de stock), navigateur réel (Playwright) sur les 4 pages avec données réelles ; base remise à l'état seed après nettoyage des données de test. Comptes seed : admin@remed.ht / pharmacien@remed.ht / vendeur@remed.ht.

  **Cahier des charges élargi reçu et fusionné (2026-08-10, suite)** : Jaslin a fourni un prompt très détaillé demandant une stack différente (NestJS/MySQL/React+Vite) et une portée multi-pharmacie. Conflit signalé explicitement plutôt qu'exécuté aveuglément ; clarifié avec Jaslin : **stack existante conservée**, le cahier des charges devient la source fonctionnelle enrichie fusionnée dans `PLAN.md` (10 phases). Décisions actées : RBAC en rôles enum fixes (pas de moteur de permissions granulaire, cohérent avec NEXORA), tests automatisés et Docker repoussés et signalés (aucun projet du portefeuille n'en a), multi-devises reporté. **Phase 0.5 (multi-pharmacie) livrée le jour même** : modèle `Pharmacie`, `pharmacieId` scopé sur les tables propriétaires (résolu depuis le JWT, jamais un paramètre client, même garde que SHOPAY), tables enfant héritant le scope sans colonne redondante. **Isolation cross-tenant vérifiée et prouvée** (deuxième pharmacie de test : listes vides, lecture par ID → 404, ajustement de stock → 404, dashboard à zéro).

  **Phase 1 (Ventes/POS) livrée le jour même (suite)** : modèles `Client`, `Paiement` (paiement scindé, plusieurs modes par vente), `CaisseTransaction`. POS complet (`/ventes`) avec **FEFO réellement implémenté** (répartition automatique sur plusieurs lots si besoin, compare-and-swap par lot), **blocage effectif de la vente si ordonnance requise et absente**, paiement scindé validé côté serveur, **annulation de vente** restituant précisément le stock et contre-passant la caisse (réservée aux rôles de gestion). Caisse : ouverture/fermeture avec calcul automatique du solde théorique et de l'écart, vente bloquée hors session ouverte. Module Clients (CRUD + historique). **Vérifié en conditions réelles de bout en bout** (curl : blocage ordonnance, FEFO, paiement scindé, RBAC, annulation avec restitution exacte, écart de caisse exact ; navigateur réel Playwright : cycle complet ouverture caisse → vente avec ordonnance → reçu → stock recalculé → fermeture caisse).

  **Achats (Phase 2), Dépenses/Retours (Phase 4 partielle), Inventaire (Phase 5) et facture PDF 80mm livrés le 2026-08-12**, sur « finaliser remed » + demande explicite de facture au format 80mm. Achats : commandes d'achat brouillon→envoyée→reçue partielle/complète, réception ligne par ligne avec numéro de lot + péremption (page `/achats`). Dépenses : catégorisées, une dépense en espèces exige une caisse ouverte et pousse automatiquement une sortie de caisse (page `/depenses`). Retours : RETOUR_CLIENT/RETOUR_FOURNISSEUR/PRODUIT_ENDOMMAGE/PRODUIT_EXPIRE/ERREUR_VENTE, sens du mouvement de stock dérivé du type (page `/retours`). Inventaire : complet ou partiel, validation génère les ajustements de stock (page `/inventaire`). **Facture 80mm** : `pdfkit`, hauteur calculée dynamiquement au contenu, `GET /api/ventes/:id/facture.pdf`, bouton d'impression sur le reçu de vente. **Vérifié en conditions réelles** (curl + navigateur réel) sur chaque module, MediaBox du PDF confirmé à exactement 80mm. **Incident d'environnement notable** : le moteur de schéma Prisma reste bloqué indéfiniment via `npx prisma` dans cet environnement (au-delà de la limitation de confirmation déjà connue) ; contourné en laissant une tentative en arrière-plan aboutir silencieusement puis en le découvrant a posteriori — détail dans `context/HISTORY.md`.

  **Phases 3, 6, 7, 8, 9 livrées le 2026-08-12 (même jour, suite), sur « PHASE RESTANTES » — REMED désormais complet sur les 9 phases du plan.** Ordonnances (Phase 3) : refonte de simple ligne-jointe-à-une-vente vers entité autonome avec statut/service partiel/pièce jointe, sélectionnable au POS sur plusieurs visites (page `/ordonnances`). Dashboard (Phase 6) : CA/bénéfice/dépenses par période avec 3 graphiques SVG maison (skill `dataviz` chargée, pas de nouvelle dépendance). Rapports (Phase 7) : page à onglets Ventes/Stock/Achats/Finance avec export CSV. Notifications (Phase 8) : cloche dans le header, alertes stock bas/péremption formalisées en table consultable/marquable lue — **bug réel trouvé et corrigé** (la déduplication ne regardait que les notifications non lues, donc "tout marquer lu" était instantanément annulé par la resynchronisation paresseuse ; corrigé en dédupliquant sur une fenêtre de 24h). Sécurité (Phase 9) : verrouillage de compte après 5 échecs (30 min, testé réellement), un trou de couverture d'audit trouvé et corrigé (création de catégorie). **Second incident d'outillage découvert** : un `tsc --noEmit` en arrière-plan a rapporté un succès (exit 0) sur un fichier qui faisait pourtant planter `ts-node` au démarrage réel du serveur (cast `req.query` invalide) — corrigé, et le démarrage réel du serveur ajouté comme vérification systématique en plus de `tsc` désormais. **Reste hors scope, assumé** : Phase 10+ (tests automatisés, Docker, multi-devises — jamais demandés).

  **Gestion des utilisateurs livrée le 2026-08-12 (même jour, suite)**, sur « qu'en est-il de la gestion des utilisateurs ? » — trou réel repéré (aucune interface pour créer un compte employé, jamais dans les 10 phases du plan). Module `/utilisateurs` (SUPER_ADMIN/GERANT) : CRUD comptes, mot de passe initial fixé par l'admin, anti-escalade de privilèges, anti-auto-verrouillage, garde du dernier SUPER_ADMIN, révocation immédiate des sessions à la désactivation/reset. Vérifié en conditions réelles (serveur démarré réellement, pas seulement `tsc`) : escalade de privilèges refusée, auto-modification refusée, désactivation bloquant immédiatement la connexion, reset de mot de passe fonctionnel, RBAC confirmé. Jamais commité en git.

---

## Mes outils et préférences

### Outils que j'utilise au quotidien

- **VS Code** pour le développement
- **GitHub** pour la gestion de versions et le stockage de code
- **Claude Code** et **ChatGPT** comme assistants de développement et de productivité
- **Postman** pour tester les API
- **MySQL**, **PostgreSQL** et **Prisma** pour les bases de données
- **Figma** pour le design d'interfaces
- **Docker** pour la conteneurisation
- **Node.js**, **Next.js**, **React**, **React Native** et **Django** pour les projets web et mobiles
- **Microsoft Word**, **Excel** et **PowerPoint** pour la documentation et les présentations

### Style de communication préféré

Un mélange selon le contexte :
- **Direct et efficace** pour les décisions, le débogage et les problèmes techniques
- **Détaillé et pédagogique** (avec exemples concrets) pour l'apprentissage de nouveaux concepts, l'architecture logicielle et les sujets complexes

### Domaine où j'ai besoin du plus d'aide

**Architecture et développement de mes solutions SaaS**, en priorité :
- L'architecture logicielle et la conception des bases de données
- Le choix des technologies et des bonnes pratiques de développement
- La planification des fonctionnalités (MVP vers version complète)
- La mise en place d'une architecture SaaS multi-clients (multi-tenant)
- La sécurité, les performances et la scalabilité
- La stratégie de lancement et de commercialisation des produits

En second plan : améliorer ma productivité pour mieux équilibrer études, projets freelance, enseignement et développement de mes produits SaaS.

---

## Notes importantes

> Cette section se remplira au fil du temps avec les éléments de contexte qui émergent naturellement dans mes sessions avec Claude.
